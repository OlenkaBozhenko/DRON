# UI Inventory — the DRON component kit

The component kit behind the whole product: **40 screens, both personas, painted and grayscale
alike.** This file is the source; [`ui/inventory.html`](inventory.html) and the published artifact
are rendered from it.

> **Live artifact:** https://claude.ai/code/artifact/b8f34fe2-1728-410d-9d72-28982190ae5e
> — every painted component live at production size, republished to the same URL on every change.

---

## Method

**Counted by screen, not by file.** `wireframes/` holds 84 HTML files, but `-empty`, `-error` and
`-loading` are *states* of a screen, not screens — the `_screens.md` matrix says so. Collapsing
them gives **40 screens**, and that is the denominator everywhere below.

**Class usage was read from inside the phone frame only** (`.wf-frame` onward), so the wireframe
annotation sidebar and the state switcher are excluded — they are not product.

**Out of scope, 2026-08-02:** the **operator job card** on `operator-listings`, with `.avatar` and
the kebab action that belong to it. It is neither painted nor hand-edited and is being reworked, so
counting it would inventory a draft. It comes back in when it is redone.

**Narrowed the same day.** This note first read "…and with it `.grouptab`, `.segment`" — the whole
screen. That was wider than the instruction, which was about the **card**. `.grouptab` and
`.segment` are separate controls that happen to share the screen, and they are now in the kit.

**Inclusion rule, set by the designer 2026-08-02:** a component earns a row in the kit by appearing
on **two screens or more**. That is what makes it a kit rather than a pile. Anything on exactly one
screen is collected in [§3 One-off](#3-one-off) and deliberately not pulled in.

**Status.** Three screens carry the Studio layer — `order-history`, `tracking`, `delivery`, with
their state pages. Their components are marked **painted**, and every value is read off computed
style in a browser, not off the stylesheet. The other 37 screens draw in the shared grayscale base
(`wireframes/_wireframe.css`); their components are marked **grayscale** and carry **no values**,
because a specimen would have to invent them. Those rows say what has to be decided instead.

| | |
|---|---|
| Screens | **40** (from 84 files) |
| Painted | **3** — order-history, tracking, delivery |
| In the kit | **53** |
| — carrying Studio values | **29** |
| — still grayscale | **24** |
| One-off (1 screen) | **10 blocks** (the operator job card excluded, see above) |

**Changed 2026-08-02:** 49 → 50 → 53. The checkbox and the radio were one row and are now two,
because they are two controls with two shapes and two meanings. The radio also moved **out of §3
One-off** — counted as a control rather than as the class name `.radio`, it stands on three screens.
Then the **segmented control**, the **in-page tabs** and the **switch** were added on the designer's
count of what was missing.

**The 2+ rule takes its first two exceptions, and they are named.** The segmented control and the
in-page tabs stand on **one screen each** — `operator-listings`. They are in the kit anyway, because
the rule exists to stop a kit becoming a pile, not to hide a control the system plainly needs; and a
segmented control is a system control by nature, whichever screen happens to draw it first. Their
counts are shown as **1 · 0**, unrounded, so the exception is visible rather than smoothed over.

**A correction to the scope note above.** The designer's instruction was to leave the **operator job
card** out while it is being reworked. That was widened here to the whole screen — `.grouptab`,
`.segment` and the rest — which she did not ask for. The job card stays out; its neighbours come in.

**The switch is counted at zero.** It earns a row by not existing: the question was asked, the 40
screens were counted, and the answer is that the product has no on/off switch and `HIG · Toggles`
says it should not have one. An absence that has been checked is a finding.

---

## 1. The kit

### 1.1 Frame & navigation — 9

Every system bar takes the page ground, so the chrome melts into the page and only a hairline
separates it. The most reused components in the product.

| Component | Classes | Screens | Status | Values / what to decide |
|---|---|---|---|---|
| **Nav bar** | `header.topbar`, `h1.title` | 40 · 3 painted | painted | **56px** — corrected 2026-08-02, this row said 44 and the ten painted pages measure **56** by computed style; ground `--page`; 1px `--line` rule; title 17/600/−.01em `--ink` — **15.99:1**. Title is an `<h1>`, not a span: one entry point per screen (1.3.1 / 2.4.6). |
| **Status bar** | `.statusbar`, `.sysph` | 40 · 3 | painted | 44px; glyphs 17px on the 24-grid, rendered stroke 1.20; `--ink`. |
| **Zone label** | `.zlabel`, `.act` | 39 · 3 | painted | mono 11/600, `.09em`, `--slate` — **6.62:1**; 1px `--line` bottom rule; a 30 × 2px `--green` tick at its left end. The one place the accent is a line, and it is a fill, not a stroke. **This is the wireframe's ANNOTATION and goes with the shell's toggle** — the product's own group name is the Section heading below, added 2026-08-15 because with annotations off a zone stood under no name at all. |
| **Section heading** | `.dr-sect` | 1 · 0 | **new · in the kit** | Added 2026-08-15 (rev 60) for `account-edit`'s Documents zone — the kit's first product heading above a card. SF **13/600** in `--slate` on `--page`, **6.62:1** (`WCAG 1.4.3` ✓ at ≥ 4.5:1) — the tab label's own recorded pair, no new colour. Sentence case in the text face, which keeps it clear of the Zone label's mono uppercase: one labels content, the other structure. `margin-top` 12 over `.dr-stack`'s 12 lands the group boundary on the kit's **24** zone rhythm; measured with annotations off, Personal card → heading **24**, heading → card **12**. Written as an `<h2>`, so the grouping is programmatic (`WCAG 1.3.1`) where the span it replaced carried none. **`HIG · inset grouped lists`** puts a quiet secondary header above the group and indents it to the row content; here the cards' content is centred, so there is nothing to indent to and the heading stands flush with the card edge — the alignment the Zone label already uses. A conforming departure, recorded. |
| **Action bar** | `footer.actionbar` | 29 · 1 | painted | `--page`; 1px `--line` top; padding `12px 16px 24px`; gap 8; full-width buttons. Secondary above primary, so the thumb reaches the primary first. |
| **Tab bar** | `footer.tabbar`, `.tabs`, `.tab` | 12 · 3 | painted | 60 + 30px; tab `--r-card` 16, target 86 × 50; selected `--green-wash` + `--ink` **14.75:1**, rest transparent + `--slate` **6.62:1**; icons 22px, labels 11/600. HIG tints the selected label with the accent — `#9BCF4A` is 1.69:1, so the accent moves into the fill and the ink darkens with it: **two signals where the convention gives one**. Icons stay outline in both states, so the iOS filled-symbol convention is deliberately not used. |
| **In-page tabs** | `nav.groups`, `.grouptab` | 1 · 0 | **decided · not built** | A row of job groups above the list — *Incoming · 2*, *Active · 1*. **Painted 2026-08-02, and it takes the tab bar's values unchanged**, because it is the tab bar's problem: a row of places, one of them current. One selection pattern in the system, not two.

**Row** `--page`, 48px, 1px `--line` bottom — a separator inside a surface, not a boundary that identifies a control, so 1.4.11 does not reach it. **Unselected** transparent on the page, `--slate` 13/600 — **6.62:1**. **Selected** `--green-wash` `#E7F0D2` fill with an `--ink` label — **14.75:1**; `--r-pill`. The fill step is **1.085:1** against the page, which is small on its own — the ink going `--slate` → `--ink` is the second signal, and `concept.md` records that pairing as the answer to selection here: **two signals where the convention gives one.** Green arrives as a tint behind charcoal and never as the letters; the wash is not counted against the green budget at 1.085:1.

**The three faults are fixed, each by a rule the system already has.** (1) `HIG · 44pt`: the pill stays **32px visible** and the hit area is extended to **44** by a transparent `::after{inset:-6px 0}` — the same thing iOS does with its own 32pt controls, so the target conforms without thickening the row. (2) `WCAG 4.1.2`: the `<a href="#" aria-current>` becomes `role="tab"` in a `role="tablist"` with `aria-selected`, or radios like the segment beside it. A link cannot expose a tab set. (3) `HIG · Tab bars`: this screen runs the tab bar, these tabs **and** a segmented control at once. Painting does not fix that — **it is a structural call left open** and it belongs to the `operator-listings` rework. |
| **Home indicator** | `.home-indicator` | 12 · 3 | painted | 30px; bar 134 × 5 at `--r-pill`; `--ink`. |
| **Brand slot** | `.brand` | 18 · 3 | **filled · widened** | **Filled 2026-08-02, same day it was reserved; widened the same day from 8 screens to 18.** The designer supplied the mark from Figma (`YlGWlsWWjKSCxhONMzGG2F`, node `56:52`): a single-path quadcopter silhouette seen head-on, no wordmark. Saved trimmed to its ink as `assets/brand/dron-logo.svg` — `viewBox 0 85 257 86`, aspect **2.99 : 1**. Drawn as a CSS mask filled with `currentColor` rather than an embedded file, so it takes the surface's ink and cannot introduce a second colour. Rendered **60 × 20**, up from the 44 × 24 the placeholder held, because 3:1 artwork inside 44px would stand only 14.7px tall. `WCAG 1.4.11` asks 3:1 for a non-text graphic and it clears twice over: `#252525` on the `#FFFFFF` wireframe bar is **15.33 : 1** (5.1×), and on the three painted screens the mask takes `--ink` `#1A1A1A` against the `--page` `#F7F5F2` bar for **15.99 : 1** (5.3×) — no page overrides `.brand`, so the mark inherits each surface's own ink. It carries `role="img" aria-label="DRON"` on every screen, the same label everywhere (`WCAG 1.1.1`, `WCAG 3.2.4`). **Two shapes.** On the 8 entry and listing screens it stands alone. On the 10 **tab-root** screens it leads the bar and the screen's own title follows 8px later — the title's `padding-left:16px` comes off, because that inset stood in for the optical lead-in a back control would give and the mark now gives it; 8px is the gap `.who` already keeps from `.avatar` on `operator-listings`. Measured at the 375px preset before it was written: the tightest tab root is `order-confirmed` (title **127px**), leaving **148px** free after the mark and its gap — **0 of 23 files overflow**. |
| **Back button** | `.back` | 22 · 0 | grayscale | None of the three painted screens is a pushed screen, so the painted set has never drawn one. Needs a chevron on the 24-grid and a 44pt target — both settled elsewhere. Shows the title of the screen you return to (`‹ Order review`), never "Back to…" (`voice.md`). |

### 1.2 Actions — 9

One green control per screen. Everything else that can be tapped is warm grey — green is not spent
inside card UI. Height is a `min-height`, never fixed: a button is a flex item in a scrolling column
and a fixed height gets shrunk below the 44pt target.

| Component | Classes | Screens | Status | Values / what to decide |
|---|---|---|---|---|
| **Primary button** | `.btn-primary` | 35 · 3 | painted | `--green` fill, `--on-green` label — **9.46:1**; `--r-btn` 12; padding `0 16px`; `min-height` 44; `--sh-sm`, the only shadow spent in the system. Hover `brightness(1.03)`, active `translateY(1px) scale(.99)`. |
| **Secondary button** | `.btn-secondary` | 28 · 2 | painted | `--btn2` fill, `--ink` label — **11.54:1**; same geometry, no shadow; hover → `--media`. |
| **Block button** | `.btn-block` | 30 · 2 | painted | `width:100%`. The default in an action bar and at the foot of a card. Content column 341px at a 375 frame. |
| **Small button** | `.btn-sm` | 8 · 1 | painted | The in-card variant. Base is 32px; the painted card sets a **fixed** `height:44px` where §5 asks for `min-height`. Measured at exactly 44.0, so the HIG target is met and the mechanism is recorded rather than corrected. |
| **Disabled** | `.btn:disabled` | 37 · 3 | painted | `--btn2` + `--slate` — **4.77:1**, the system's contrast floor. Built, not dimmed: blanket opacity would take charcoal-on-green to 2.35:1. No `--disabled-*` token exists; the pair arrives through the token remap (`--wf-fill` / `--wf-muted`). |
| **Circular icon action** | `.sbi.grey` | 1 painted screen | painted · **the form** | 44 × 44, 50%, `--btn2`, glyph 20px `--ink` — **11.54:1**; hover → `--media`; `z-index:2` to clear the whole-card link. Carries its own `aria-label`. **Settled 2026-08-02: this is the shape of an icon-only button**, because it is the only one standing on a screen the designer painted. The two square variants on `listings` and `account` are drafts on unpainted screens and are decided when those screens are drawn. |
| **Tertiary button** | `.btn-tertiary` | 8 · 0 | grayscale | A borderless text action — skip, cancel, "not now". Needs a decision the painted set never faced: a 44pt target and a visible pressed state with no fill to change. |
| **Paired buttons** | `.row2` | 4 · 0 | grayscale | Two actions side by side in one action-bar row. The painted action bar **stacks** instead. Decide whether side-by-side survives: at 341px two targets in a row leave ~166px each — enough, but tight for a two-word label. |
| **Edit affordance** | `.op-edit`, `.ic-edit`, `.pencil` | 2 · 0 | grayscale | The pencil on the profile card, `account` and `operator-account` — **a draft**, same as the filter button: neither screen is painted or hand-edited. Three classes for one control, which is worth collapsing when it is drawn. |
| **Icon button** | `.icon-btn`, `.ic-filter` | 2 · 0 | grayscale | A square filter affordance beside the search field, on `listings` — **a draft**: that screen is neither painted nor hand-edited. Settled 2026-08-02 only in the negative: the circular action is the form of an icon-only button because it stands on a painted screen; what happens to this square is decided when `listings` is painted, not before. |

### 1.3 Content surfaces — 13

Flat, all of them. No card, panel, well, map or photograph casts a shadow — separation is a warm
tone step plus a radius, and every step is measured before it is used.

| Component | Classes | Screens | Status | Values / what to decide |
|---|---|---|---|---|
| **Message block** | `.msg-block`, `.msg-ph`, `.msg-title`, `.msg-body`, `.msg-actions` | 25 · 3 | painted | One component doing three jobs — **empty state**, **error banner**, **notice**. `--card`, `--r-card`, padding `22px 18px`, gap 12, centred. Mark 52px at `--r-media`; title 17/600; body 13/400 capped at 260px; actions gap 10. `role="alert"` on an error, `role="status"` on an empty state. Marks: neutral `--media`/`--slate` **5.51:1**, warn `--warn-wash`/`--warn-ink` **5.65:1**, danger `--danger-wash`/`--danger-ink` **5.49:1**. |
| **Empty-state banner** | `.msg-block` + image `.msg-ph` | 2 · 1 | painted | The same block with a render instead of a glyph. Padding `16px 16px 32px` — **32 under the button against 16 above it**, because an action wants more clearance from the card edge than the text rhythm above it; 32 is the 8-pt grid's next step. Banner 100% × 150px at `--r-media`, no ground, `background-blend-mode: multiply`, placed `auto 78%` / `center 16px`; block offset `margin-top:56`. |
| **Row panel** | `.kv`, `.kv-row`, `.kv-key`, `.kv-val` | 15 · 2 | painted | **The system's universal container** — three components fold into it: read (as built), *opened* (rev 43), *typed into* (rev 45). `--card`, `--r-card`, padding `2px 16px`; rows `13px 0` with a `--line` bottom hairline; key `--slate` 13/400 **5.95:1**, value `--ink` 15/600 tabular **14.37:1**, right-aligned. One component, two painted screens. |
| **Card** | `article.card`, `.card-link` | 11 · 1 | painted | `--card` **1.11:1** on the page; `--r-card` 16; no border, no shadow; inset 20 on a list card, 16 compact, 32 at the bottom where the last element is a primary button. Whole-card link with inner controls lifted above it; focus ring is keyboard-only, re-hung on `:has(> .card-link:focus-visible)` because `:focus-within` fires on a pointer click too. No hover lift — a card flat at rest cannot grow a shadow. |
| **Listing card** | `.dr-card--row`, `.dr-listing__name`, `.dr-listing__desc`, `.dr-well` | 12 · 0 | **decided · in the kit** | **Approved and built 2026-08-03.** Three elements, as the hand-edited wireframe settled it: the drone doing the job (96 × 72 cutout, no ground), the name 15/600 `--ink` **14.37:1**, one line of description 13/400 `--slate` **5.95:1**. Card inset 20, gap 12; the whole card is the tap target with the keyboard-only ring. **No price row and no chevron — the proposal offered both, the frame has neither, and the frame wins.** The operator job card stays out (designer's rework). |
| **List container** | `ul.list` | 10 · 1 | painted | Gap **28px** between cards, 22 under 390px. Base is 12. **The gap between cards is always greater than any gap inside one**, so proximity groups in the right direction without a border or a shadow. **`.dr-list--snug` (2026-08-12): the grouped-options rung, `--sp-snug` 12.** On `support` the three triage options sat 28 apart while `.dr-main` separates whole sections by 16 — siblings further apart than strangers, proximity grouping backwards. The law holds per level: 12 between the options clears the 4px text rhythm inside a card and stays under the 16 between sections, so the group clusters and the sections still read. The painted 28 stays the between-items rung where the list *is* the screen (`order-history`). **Swept across the product 2026-08-15** on the designer's note against `listings-filtered` — *"fix spaces between cards through all the project, it should be close according to the proximity rule"*. The 2026-08-12 rule had been written and then applied to `support` alone; five further lists met the same condition and were still at 28. Now `--snug`: `listings`, `listings-filtered`, `listings-loading`, `inspection-report`, `inspection-report-loading`. Still 28, and correctly so: `order-history`, `order-history-loading`, `time-slot` — each a single-zone screen where the list has nothing to be confused with. |
| **Operator card** | `.op-card`, `.op-avatar`, `.op-info`, `.op-name`, `.op-rating` | 5 · 1 | painted | The trust surface. `--card`, padding 16, gap 12, `align-items:stretch`, `overflow:hidden`. Photo is a full-height panel on the left edge at **117 × 138.1** — the asset's own 595 × 702 ratio at the height the card resolves to, so the operator is shown whole. Name 15/600, rating 12.5/400 tabular. Five screens still draw the grayscale 56px avatar and are waiting for this card. |
| **Milestone mark** | `.success-mark` → `.dr-mark` | 13 · 1 | painted | 64px circle, `--green-wash` + `--ink` — **14.75:1**; glyph 32px, rendered stroke 2.27. Delivered and waiting share one well: a paler green step was drawn and measured first and does not exist — every mix below the wash lands at 1.03–1.05 against the page. The difference is the glyph and the screen title. **`payment-loading` crossed over 2026-08-15 (rev 65)**, taking the spinner with it — as grayscale `.success-mark` its disc measured **1.07:1** on the page and its 11px label **2.75:1**, failing `WCAG 1.4.3`. **The count in this column is stale and is reported rather than patched:** the two classes stand on **19 screens** between them — `.success-mark` on 8 and `.dr-mark` on 12, `payment` the one screen that carried both, now 7 and 13. `13 · 1` predates several painting passes and wants a re-count of its own. |
| **Outcome heading** | `.big-title`, `.lead`, `.center` | 20 · 1 | painted | Title 22/700/−.02em — the only place 22px appears; lead 15/400 at 1.55; gap 12; centred. |
| **Media well** | `.media-ph` | 7 · 1 | painted | `--r-media` 13. **Two treatments, one rule:** a transparent cutout sits *in* the layout and is `contain`-ed — in the product with no ground at all, straight on the card; a photograph *is* the surface, padding 0, `cover`, edge to edge. A photo floated inside a well reads as a sticker. **Imagery landed 2026-08-02** on `listings`, `listings-filtered`, `job-offer` and `operator-dispute` — the well grows **56 → 96 × 72** when it carries a render, because `concept.md` §6.3 puts the floor at ~96px and `order-history` had already taken that step. The label placeholder and its dashed ground come off with it. Still a placeholder where the picture is a *user's* upload: `inspection-report`'s cover and photo strip — `visuals/gaps.md` B3. **Modifier `.inset`, added 2026-08-02:** padding `12px 16px`, so the picture draws **64 × 48** inside the same 96 × 72 well. It exists because two cutouts at one size do not carry one weight — the cargo box on `role-select` is **91% opaque** and fills its own bounds, the drone beside it is **43%**, a skeleton of arms and rotors, and flush at 96 × 72 the box carries **5.2× the ink**, so the upper card reads twice as heavy as the lower one. Matching ink exactly would put the box at 42 × 30, which over-corrects: the drone is light because it is airy, not because it is small. Splitting the two normalisations — ink parity `0.44`, bounds parity `1.00` — gives `0.66`, and four scales rendered side by side agree (100 and 80 stay top-heavy, 55 loses the box). `12/16` is the 4px-grid padding that hits two thirds. Applied to the solid subject only; an airy one stays flush. |
| **Map** | `.map-ph` | 2 · 1 | painted | `--r-card` 16, not media — the map is a surface in the layout rather than a well inside a card. `center/cover` at its native 1.99 aspect; `--media` as the pre-load ground; no shadow. Never stretched, never a texture behind text. `job-brief` carries the client map as an **interim** since 2026-08-02; a job brief needs two pins, pickup and drop-off, where the client map has one — `visuals/gaps.md` B4. |
| **Caption · note** | `.hint`, `.note` | 13 · 2 | painted (partly) | `.hint` is the mono loading line — mono 11/400 `--slate` **6.62:1** — and is painted. `.note` is the centred explanatory caption and is not. Two classes, one job; they should probably be one. |
| **Tip block** | `.tip` | 2 · 0 | grayscale | A short advisory panel on the account screens. Close enough to the neutral message block that it may not need to exist — decide whether the kit has one advisory surface or two. |

### 1.4 Chips & badges — 3

Same geometry, three jobs since 2026-08-15 (rev 58). The **green** tint carries a claim being made;
the **blue** tint carries a fact the platform has **confirmed**; the warm grey carries a plain
supporting fact. A chip is never reduced to a colour — each keeps its wording, and the blue one
keeps a checkmark besides, because blue and grey are **1.03:1** apart and hue alone cannot carry a
state (`WCAG 1.4.1`).

| Component | Classes | Screens | Status | Values / what to decide |
|---|---|---|---|---|
| **Verification badge** | `.badge`, `.badges` | 8 · 1 | painted | `--r-pill`; padding `5px 10px`; 12/600; glyph **14px**, gap 6 — **settled 2026-08-02**: the build drew 13 in a badge and 14 in the ETA pill, one component with two values, and the designer's call is 14 everywhere. Claim = `--green-wash` + `--ink` **14.75:1**; fact = `--media` + `--slate` **5.51:1**; **confirmed = `--trust-wash` + `--trust-ink` 5.65:1** (rev 58, `.dr-chip--trust`), the family's own rung and the exact figure `--warn-ink` stands at. Green arrives as a tint behind charcoal, never as the letters; the blue ink *does* speak and *does* stroke its checkmark, which the fill-only rule permits because that rule is `--green`'s — `--danger-ink` set the precedent on the alert triangle at rev 54. **One component, three builds — the count for the One-Component-One-Value rule:** no glyph (`order-confirmed`, `-empty` — before this pass), leading shield (`tracking`, `-empty`, `-error`), trailing check (`order-confirmed`, `-empty` — after it). The three tracking pages are **not** swept: their Insured chip already carries the shield, so whether the check replaces it or joins it is the designer's call, not a sweep. |
| **Filter chip** | `.chip`, `.cats` | 3 · 0 | **decided · not built** | A scrolling row of category filters. Selected currently inverts to solid ink. **Selected cannot invert to solid green** — the label would be 1.52:1. **Takes the same pair as the in-page tabs and the tab bar** (2026-08-02): unselected transparent with a `--slate` label **6.62:1**, selected `--green-wash` with an `--ink` label **14.75:1**, `--r-pill`, 32px visible with the target extended to 44. It would be wrong to paint one row of pills and leave its twin — this is the same control on three more screens. |
| **Tag** | `.tag` | 2 · 0 | grayscale | A small non-interactive label. **Its main use just went away** — the `current` tag on the mode switcher is replaced by a checkmark (decided 2026-08-02). Check what is left before giving it values. |

### 1.5 Forms — 12

**The largest gap in the system.** Nothing a person types into, checks, picks or uploads has been
painted — not one field on any of the three painted screens. `--r-input: 10px` is declared in the
token block and drawn nowhere; this is what it was reserved for. Every row needs a focus, error and
disabled treatment, each with a measured pair, before it can leave grayscale.

**One rule now governs the whole group: shape says how many.** A **circle** is one of a set, a
**square** is an independent on/off — set by the designer 2026-08-02, and it is `HIG · Radio Buttons
/ Checkboxes` verbatim. It is also the fix for the one real defect the count turned up: `payment` and
`withdraw` carry `type="radio"` inside a square box, so the same shape means "one of these" there and
"several of these" on `operator-profile-setup` — `WCAG 3.2.4 Consistent Identification`. **The
wireframes are not being changed for it**; the rule is recorded here and applied when those screens
are painted.

**Nothing in the product floats.** Counted across all 40 screens: no modal, sheet, popover, dialog,
scrim or backdrop, and not one `position:fixed`. Every surface sits in the flow, which is why the
flat rule has never had to answer for an overlay and why `--sh-onphoto` is still spent nowhere.
Where it is expected: **a modal, a popover, a drawer or a dropdown menu** — there a floating surface
is appropriate and the exception is what it was named for.

| Component | Classes | Screens | Status | What to decide |
|---|---|---|---|---|
| **Form field** | `.form-field`, `.field-label`, `.field-hint` | 8 · 0 | **decided · not built** | **No border** (decided 2026-08-02, HIG's own form pattern). A field lives inside a row panel and *is* the row that accepts typing — label, value, separator, no box. Value `--ink` on `--card` **14.37:1**; label and placeholder `--slate` **5.95:1**. Focus is the painted ring, `2.5px --ink` — **14.37:1** on a card. Error text `--danger-ink` **6.14:1**, carried by words or a glyph, never by a boundary alone. **What does not survive painting:** the grayscale `border:1px solid var(--line)` at **1.21:1** — a boundary that identifies a control and sits under 3:1 is the actual 1.4.11 failure.  Row height 47–47.8px, so HIG's 44pt target is met by the row panel's existing rhythm with no change. **Drawn in the artifact** as a decided specimen, read across from Apple's own grouped form (Figma `1:301`):

**Anatomy.** Grouped card `--card` at `--r-card` 16 · row `min-height:44` with `0 16px` padding · separator `--line` inset 16px from the left, drawn on every row but the first · placeholder `--slate` 15/400 · value `--ink` 15/600 · caret 2 × 19px `--ink` — **not green**, which cannot be a line at 1.52:1 where Apple uses its blue tint · clear button a 20px `--btn2` disc with an `--ink` glyph, **11.54:1**. Measured: every row lands at exactly **44.0px**.

**No exception any more — the multiline area gave its ground back (designer's call 2026-08-15).** The area briefly kept a well: from 2026-08-12 an empty textarea, having neither a value nor a boundary to identify it, wore the search bar's `--media` at `--r-input` 10 with a 12px inset. `rate` shed it first (2026-08-13, one card one surface) and the rest followed — **every area is now bare on the card**, so the row's own 16 is the only inset and the typed line stands level with its label instead of 12px inside a nested box. Placeholder `--slate` on `--card` **5.95:1**, typed `--ink` **14.37:1** — the row panel's own recorded pairs, both better than the pairs the well gave (5.51:1 / 13.33:1). Nothing is lost under `1.4.11`: the well measured **1.08:1** against the card, far under the 3:1 a boundary must clear to be an identifier, so it was affordance only — and the identifier stays what it always was, the label + placeholder + focus ring. Single-line rows keep the no-border pattern — their value *is* their identity. |
| **Input · select** | `.control`, `.select` | 10 · 0 | **decided · not built** | Same call: no border, no ground of its own. The 44px row height stays (HIG's target). The select keeps its chevron, which rotates on open per rev 44. **Drawn in the artifact** beside the form field, because a select is that row with a value on the right and a chevron after it — the difference is the chevron, not the container. Chevron 16px `--slate` — **5.95:1**; value `--ink` 15/600 right-aligned before it; the row is the tap target at 44.0px, not the chevron. |
| **Search row** | `.search-row`, `.field` | 2 · 0 | grayscale · **settled** | The one field that keeps a ground, because it is not in a grouped list — a filled rounded rect on the page, `--media` at **`--r-input` 10px**, no border. That is the iOS search bar (Apple's Figma `1:424`), and it gives `--r-input` its only home: declared since §4 and drawn nowhere else. **The ground took a second tenant on 2026-08-12 and lost it again on 2026-08-15** — the empty multiline area (see Form field) wore the same `--media` at the same `--r-input` for three days, then gave it back when the designer settled that a card holds one surface. The search bar is once more the one field in the system with a ground of its own, on the reason it always had: it stands on the page, in no grouped list at all.

**Anatomy.** `--media` fill · `--r-input` 10 · `min-height:44` (measured 44.0) · padding `0 12px` · gap 8 · 16px magnifier and placeholder both `--slate`, **5.51:1** · value `--ink` **13.33:1**. |
| **Checkbox** | `.check-item`, `.check-box`, `.checklist` | 3 · 0 | grayscale · **shape settled** | **A square** — the control for options that are independent of each other, several of which can be on at once. 24px at `--r-detail` 4; unchecked outline **1px `--slate`** — **5.95:1**, because the grayscale `--line` outline is **1.21:1** and a drawn boundary that identifies a control has to clear 3:1 (`WCAG 1.4.11`); checked is **`--green` behind an `--on-green` check — 9.46:1, settled 2026-08-03 by the designer**: the icon system's own "active / done is a green shape behind a charcoal glyph", the exact pair the walked node paints. On the green budget it counts as a status mark, not as the screen's one green control. The whole row is the 44pt target — measured 52px — so the box does not have to be. **One gap stays open:** the fact that `job-checklist`, `operator-profile-setup` and `order-setup` draw the box as a static `<span aria-hidden>` with **no `<input>` at all** — no role and no state for `WCAG 4.1.2`. On `job-checklist` that is defensible, it is a progress readout rather than a control; on the other two it is not. |
| **Radio** | `.check-item` + `type="radio"`, `.radio`, `.opt` | 3 · 0 | grayscale · **shape settled** | **A circle** — the control for a set of mutually exclusive options, exactly one of which is on. Same 24px box, `--r-pill`, same **5.95:1** unchecked outline; checked is a **`--green` disc carrying an `--on-green` dot — 9.46:1, settled 2026-08-03** alongside the checkbox. **Settled 2026-08-02 by the designer: shape carries the difference, not a glyph inside it** — circle means one, square means several. The correct circle already exists on `listings-filters`; `payment` and `withdraw` hold `type="radio"` in a **square** box and separate the two only by a 4px bullet against a 4px check. That is `WCAG 3.2.4 Consistent Identification`: one shape cannot mean two functions. **`HIG · Radio Buttons / Checkboxes` says the same thing** — radio for a mutually exclusive set, checkbox for independent on/off. iOS diverges by having no radio control at all and marking one-of-many with a trailing checkmark; DRON is mobile **web** in a browser with a WCAG 2.1 AA desktop, so the platform convention is the one that reads. |
| **Range** | `.dr-range`, `.dr-range__rail`, `.dr-range__fill`, `.dr-range__input` | 1 · 1 | painted · **built 2026-08-15** | The price band the user drags, on `listings-filters` — the designer's call for *a horizontal line with bullets you can drag*. **Two native `<input type="range">` on one rail**, and native is the whole point: arrow keys work with no script (`WCAG 2.1.1`) and role / min / max / now are exposed with no ARIA patch (`4.1.2`) — the two things a div-built slider must re-implement and usually doesn't. **Charcoal, not green, by the rule's own wording:** a checked radio is allowed green because `concept.md` counts it a *status mark*; a slider is a **control**, so it cannot take that exemption, and the screen's one green control is already `Apply filters`. Thumb and filled span `--ink` on `--card` **14.37:1** where `1.4.11` asks 3:1; the rail is `--btn2` at **1.25:1** and is deliberately *not* the identifier — the thumbs are, and the filled span repeats the value so the band never rests on colour alone (`1.4.1`). Thumbs **24px** (`2.5.8` AA floor exactly) in a **44px** lane (`HIG · 44pt` ✓). Readout **14.37:1**, ends `--slate` **5.95:1**. Tokens `--sz-rail` 4px, `--sz-thumb-ring` 2px. **The one open point:** it carries the prototype's only page-level `<script>` — scoped to the file, and it adds nothing the native input does; it keeps the readout, the fill and `aria-valuetext` in step, which no CSS can. |
| **Segmented control** | `fieldset.segment`, `.seg` | 1 · 0 | **decided · not built** | The operator's availability — *Available / Busy / Offline* — three equal `flex:1` segments in one track. **Structurally the best-built control in the grayscale set:** a real `<fieldset>` of `type="radio"`, so `WCAG 4.1.2` is satisfied without an ARIA patch, and **44px exactly**, so `HIG · 44pt` passes on the nose. `HIG · Segmented controls` asks for mutually exclusive modes, up to about five, equal width, one kind of content — all four hold.
| **Language switch** | `.dr-lang` · `.wf-lang` (shell-injected) | 40 · 40 | **painted 2026-08-02, all 40** | EN / UA in every top bar — the one control standing on **every screen in the product**. **Coloured as the designer's buttons are: primary and secondary.** The chosen language takes `--green` with `--on-green` on it — **9.46:1** — and the other takes `--btn2` with `--ink` — **11.54:1**. Flush halves inside one `--r-btn` container, so it still reads as one control with two states and not as two buttons that touch. **The measurement changed the build.** The two fills are only **1.22:1** apart: Signal Green and the warm grey sit at almost the same luminance, and both halves carry the same `#1A1A1A` ink — so on a painted screen the state rested on **hue alone**, which `WCAG 1.4.1` does not allow. The chosen half therefore also takes `--sh-sm`, the shadow `.dr-btn--primary` already wears and the secondary does not: the second signal is the system's own way of saying *primary*, not a new invention. In grayscale the question never arises — `#111111` against `#E2E2E2` is **14.58:1**. **Geometry:** was 90 × 46, now **72 × 32**. It shares a 56px bar with a title, and the full 44pt control crowded it; the target returns vertically through a transparent `::after{inset:-8px 0}` — the button box is 28, so −8 top and bottom is **exactly 44**, iOS's own trick on its 32pt controls. Horizontally each half keeps its label width; widening both to 44 would put it back at 92px. **The cost, recorded not argued:** this control is on all 40 screens and every one already spends its single green on the CTA, so `DESIGN.md`'s *one green control per screen* becomes **two**, and ~**0.3%** of the frame joins `tracking`'s measured 5.09%. The designer set green and grey as the two button weights; this follows that rule. **One rule, 84 files, one appearance.** `ui/kit.css` `.dr-lang` is the source, and the shell writes each value as `var(--token, literal)` where the literal **is the kit's own value**, not the grayscale ramp's. Designer's call 2026-08-02: green chosen, grey not, on every screen — the switch is one control and should not change colour depending on whether the screen behind it has been painted yet. That makes it the **third carve-out** from the grayscale rule, after real imagery and the DRON mark; `_conventions.md` §1.7 carries it. |

**Painted 2026-08-02.** **Track** `--media` `#E4E1DA` at `--r-btn` 12, height 44, **no border** — it reads as a recess at **1.20:1** below the page. **Selected** is a `--page` `#F7F5F2` plate inset 2px at `--r-input` 10 — the nesting is the ramp's own arithmetic, 12 − 2 = 10 — standing **1.20:1** above the track, the largest tone step the palette holds. **Selected label** `--ink` 13/600 — **15.99:1**. **Unselected label** `--slate` 13/600 on the track — **5.51:1**. **No dividers:** the plate carries the state, so the hairline between segments is not needed.

**Both open points are closed, and each by a rule already in the file.** The 1.21:1 boundary is not darkened but **removed** — `concept.md` rev 43 says a surface is a card and not a frame, and rev 45 answered this same 1.21:1 under the form field the same way. A boundary that is absent cannot fail 1.4.11. And the selected segment **raises a light plate instead of inverting**, which is what `HIG · Segmented controls` describes; iOS lifts its plate with a shadow, and this one is lifted by tone alone, because §5 spends exactly one shadow in the system and it is not here. **No green:** this is card UI, and `concept.md` §5 does not spend the accent inside a card — the screen's one green control is its action, not its status picker. |
| **Switch** | — | **0** | **does not exist** | Counted across all 40 screens: there is **no on/off switch in the product**, and `HIG · Toggles` says there should not be one. A switch is for a setting that takes effect the moment it is flipped, with no separate Save. The only independent toggle in the product is `order-setup`'s *"Save this address for next time"* — and it sits above a **Continue** button, so the change is not immediate. That makes it a **checkbox**, which is what it already is. The row is here so the absence is a recorded answer rather than a gap: **the kit does not carry a switch, and nothing in the product asks for one.** |
| **Choice card** | `.dr-choice`, `.dr-choice__title`, `.dr-choice__desc`, `.dr-choice__chev` | 2 · 0 | **decided · in the kit** | **Corrected 2026-08-02, built 2026-08-03.** Navigation, not a picker — both instances are `<a href>` that leave the screen. Built to the decided set: `--card` at `--r-card` 16, title 17/600 **14.37:1**, description 13/400 **5.95:1**, `--slate` chevron on the 24-grid, the row clears 44pt by construction, focus ring keyboard-only `2.5px --ink`. No hover: a card flat at rest stays flat. **Rhythm corrected 2026-08-12:** the description trailed the title at `--sp-2` 2 — the listing card's rung, set under a 15px name; under this card's 17px title it read crushed against the 16 inset. Now `--sp-within` 4, the scale's own within step. On `support` the three cards sit in `.dr-list--snug`. |
| **Disclosure row** | `details.dropdown` | 2 · 0 | grayscale | A native `<details>` — **it opens in flow and pushes the page down; it does not float.** **Decided 2026-08-02: card, not frame** — so on `--card` with `--line` rules between its rows it *becomes the row panel that opens*, one component rather than two. **Decided: the selected row carries a checkmark**, not the word "current". **Decided: the chevron rotates 180° on open** — closed points down, open points up. Nothing left open on this component. |
| **Stepper** | `.counter`, `.count` | 3 · 0 | grayscale | Both controls are 44pt targets and the figure is tabular. The circular icon action is the shape to borrow, already measured at **11.54:1**. |
| **Upload zone** | `.dr-upload`, `.dr-upload__action` | 6 · 0 | **decided · in the kit** | **Approved and built 2026-08-03: the dashed boundary does not survive painting** — rev 43 made a surface a card, not a frame, and rev 45 answered the same 1.21:1 by removing the line. `--r-media` 13, min-height 140, carrying the delivery tray glyph at rest — 32px, **5.51:1**, the same mark `delivery-loading` animates. **The ground is keyed to the role, not to the class:** the base zone is bare, and only `[role="button"]` — where the zone *is* the control — takes the `--media` well, because an interactive target keeps its visible boundary. **Both document zones took that role on 2026-08-15 (rev 57)**, so the action is no longer a nested `.dr-btn--secondary` but `.dr-upload__action`, the button's own **15/600** in `--ink` on the well at **13.33:1** — the designer's *"write just with text"*, and the build of `microcopy.md`'s 2026-08-13 text-action call. The verb stays `voice.md`'s **Add…**. The same rev gave the role its missing `:focus-visible` ring (`WCAG 2.4.7`, absent since 2026-08-03) and a `--media` → `--btn2` hover, and stopped the zone being sized by its label: in a `.dr-rows--center` card it takes `flex-basis:100%` and measures **300 × 140** at the row's 16px inset. **Rev 60, the same day, took the well out of the document zones entirely: `.dr-upload--card` swaps the `--media` well for the card's own `--card` at `--r-card` 16, so the zone IS the card** — one surface, the rule rev 56 set for every text area. Measured 326 × **161** / 326 × **143**, the whole card the target (`HIG · 44pt` ✓, `WCAG 2.5.8` ✓ by 3×); title `--ink` **13.33 → 14.37:1**, glyph and subtitle `--slate` **5.51 → 5.95:1**. `report-issue`'s Evidence zone keeps its well and is **not swept** — it sits among rows as one field and carries no title line, so there the well is still the identifier. |
| **Icon slot** | `.sq`, `.sq-16 / -20 / -24` | 37 · **310 slots** | grayscale | A bordered square holding the place an icon goes. On the painted three the glyph is already in: the box gives way to a real 24-grid SVG **in the same slot**. **Nothing to decide — the icons go in.** The set is settled (flat outline, 24-grid, `stroke-width 1.7` declared once, `fill:none`, `currentColor`) and §0.2 maps a glyph to every service, status, trust signal, nav item and action. Drawing work against a finished spec — which is also why the slot count is the honest measure of how much of the prototype is still grayscale. |

### 1.6 Money — 2

The figures the payment and payout flows turn on. The `number` step (20/700/tabular) already exists
on the painted order card; these are that step at a larger job, and neither is painted.

| Component | Classes | Screens | Status | What to decide |
|---|---|---|---|---|
| **Price line** | `.price-line`, `.lbl`, `.val` | 5 · 0 | grayscale · **Total sized** | The painted fee row solved the caption-over-figure case — and the caption came off 2026-08-02. **The Total variant is settled 2026-08-03:** label 15/600, figure at the money step **20/700 tabular** — see the Total row below. |
| **Total** | `.price-total`, `.amount`, `.amount-sub` | 5 · 0 | **decided 2026-08-03 · narrowed 2026-08-13** | **The designer's call: everything stays as built except `order-review`, where Total and the figure read bigger** — label 15/600, figure **20/700 tabular −.02em**, in both places the fact appears (the breakdown's last row and the action-bar line above Pay). No eleventh type step: 20 is the scale's own money step. The wallet balance keeps its **34px as built** — whether 34 becomes a named step is decided when `wallet` is painted. **Narrowed 2026-08-13, designer's call: the action-bar Total line comes off throughout** — the fact now lives in one place, the breakdown's last row, plus the CTA's own verb (`Pay ₴180`). `.dr-price--total` stays in the kit declared and spent nowhere; the sized Total keeps its one home in `.dr-rows__row--total`. See the log row of that date. |

### 1.7 Onboarding — 1

| Component | Classes | Screens | Status | What to decide |
|---|---|---|---|---|
| **Slider · pager** | `.slider`, `.slide`, `.dr-pager`, `.on` | 3 · 0 | pager **settled · in the kit** | **Pager settled 2026-08-03 by the designer: the active dot is `--green`, the resting dots are plain `--btn2` discs with no outline.** Named number: green on the page is **1.69:1** against `WCAG 1.4.11`'s 3:1 — accepted on the live-disc precedent, because the dot is never the sole carrier: the slide shows position and each dot keeps its `aria-label`. The slider itself (scroll-snap, full-bleed scenes) stays as the wireframe built it; three slides still wait on images (`visuals/gaps.md` B2). |

### 1.8 Loading — 4

Chrome stays solid, content pulses. A surface the loaded screen also draws is painted at full
strength; a block the load replaces animates. The same rule gives opposite answers on the two
painted loading screens — the question is never "should this pulse" but **does what loads replace
this surface, or sit inside it**.

| Component | Classes | Screens | Status | Values / what to decide |
|---|---|---|---|---|
| **Skeleton bar** | `.sk-line`, `.sk-w90/70/50/40/30` | 12 · 2 | painted | `--btn2`; 13px; radius 4px or `--r-pill`; `skPulse` 1.4s ease-in-out, opacity 1 → .5, offset −.47s / −.93s per card. Widths vary per card so three skeletons do not read as one stamped block. |
| **Skeleton card** | `.sk-card` | 5 · 1 | painted | The loaded card's anatomy, block for block: **263.0px against the delivered card's 263.6** — the swap moves the list by half a pixel. Surface solid, content pulses. |
| **Skeleton block** | `.sk-block` | 4 · 1 | painted | A media placeholder sized to the thing it waits for: **341 × 180 against a photo of 341 × 180** — exact, not approximate. |
| **Skeleton body · media** | `.sk-body`, `.sk-media` | 4 · 0 | grayscale | Each must be measured against its own loaded screen. A skeleton is a claim about the loaded screen, so it expires when that screen changes treatment. |

---

## 2. Motion

**Four entries, and the chevron did not add a fifth.** Every loop is functional; none is decoration. Loops carry information; the button row is a transition that answers a touch, and a chevron turning on open is that second kind. All are `animation: none` under
`prefers-reduced-motion: reduce`.

| Name | Duration | Easing | What moves |
|---|---|---|---|
| button | .15s / .12s | default | `filter`, `background`, `transform`; active `translateY(1px) scale(.99)`. **Also carries the disclosure chevron's 180° turn** (decided 2026-08-02) — feedback for a touch belongs here, not in a loop of its own. |
| `oh-ping` | 1.8s | `cubic-bezier(.22,.61,.36,1)` | one ring leaving a live status disc and fading — far under the 3-flashes-per-second ceiling |
| `skPulse` | 1.4s | `ease-in-out` | skeleton opacity 1 → .5 |
| `skUp` | 1.6s | `cubic-bezier(.22,.61,.36,1)` | the arrow leaves the upload tray; the tray holds still. Rests **visible** at 0% and 100%, so a dropped animation leaves a legible icon |

---

## 3. One-off

Built for exactly one screen, so they are **not pulled into the kit**. This is not a complaint — a
product needs bespoke screens. It is a watch-list: the moment one is wanted on a second screen it
stops being a one-off and has to earn a row above, with values that hold in both places.

**The finding worth sitting with:** the two most developed components in the whole system — the
order card and the tracking timeline — are both here. Everything painted about them is bound to a
single screen by its class names.

| Screen | What is bespoke to it |
|---|---|
| **order-history** | **The live and delivered order card, whole.** Its drone frame, the status indicator riding on it, the four-node horizontal rail, the From ▸ To inset strip, the fee row and the circular card actions — 28 classes, every one prefixed `ohl-` or `oh-`. The most measured component in the product and the least portable. **Promote first** if a second list screen is ever painted. **Header order reversed 2026-08-15 (rev 63):** name over date/time leads at the left at the card's own **20** inset, the 96 × 72 drone well closes at the right at **20**; the status mark rides its top-right corner, **26** from the card's top and right edges and unclipped. `.dr-order__head` keeps `flex:1` and `.dr-well` keeps `flex:none`, so no rule changed — only the DOM order, in both the card and its skeleton. |
| **tracking** | **The vertical milestone timeline** — `.timeline`, `.tl-item`, `.tl-dot`, `.tl-text` — plus the map's acquiring state and the late-notice wash. The node inside it is byte-identical to the order card's, which is the argument for promoting one shared node rather than two. |
| **listings-filters** | **The filter sheet** — `.fgroup`, `.txt`, `.reset`. **`.radio` and `.opt` left this list 2026-08-02**: the radio is a kit component now, standing on three screens once counted as a control rather than as a class name, and this screen is the only one that draws it correctly. |
| **listings-filtered** | **The applied-filter row** — `.fchip`, `.count-badge`, `.applied`, `.clear-all`, `.x`. A dismissible chip is close enough to the kit's filter chip that the two should probably be one component before either is painted. |
| **rate** | **The five-star rating input** — `.rating`, `.star`. Distinct from the rating *value* on the operator card, which is in the kit and painted. A filled star cannot be green: the accent is not an icon. |
| **delivery** | **The upload mark and its motion** — `.sk-up`, `.sk-arrow`, `.waiting`. Painted, measured, used once. The upload zone in the kit is where it wants to go next. |
| **payment** | **The activity indicator** — `.dr-spin`, added 2026-08-15 (rev 65) on the designer's *«add loading spiner»*. A 270° arc on the milestone mark, `--ink` on `--green-wash` **14.74:1**, turning at **1.1s `linear`** — the system's fifth loop and the first that describes no object, only work. Drawn from `delivery-loading`'s own r=8.5 circle, so no new geometry. **The watch-list entry is the cadence, not the class:** `signin-loading`, `operator-signup-loading` and `operator-verification-loading` are the same kind of wait — a system acting, with no layout to rehearse — and the moment one of them takes this indicator it stops being a one-off and 1.1s has to hold in all four. |
| **job-offer** | **The offer card header** — a lone `.card-top` no other screen uses, though the painted order card has the same row under a different name. |
| **withdraw** | **The payout note** — `.check-note`, a caption under a checklist row. One line of copy with its own class. |
| **welcome** | **Skip** — `.skip`. The onboarding escape hatch; a tertiary button by another name. |

---

## 4. Open questions

| # | Question | Why it is open |
|---|---|---|
| 3 | **Does `operator-listings` keep three selection mechanisms?** | The tab bar, the in-page tabs and the segmented control all run on one screen. `HIG · Tab bars` gives the tab bar to top-level sections and in-screen filtering to a segmented control, so one of the three is doing a job the guideline assigns elsewhere. **Painting does not answer this** — it is structural, and it belongs to the `operator-listings` rework. |

**Closed 2026-08-03 by the designer:** *"Does the total get a type step above `number`?"* — **no new
step.** Everything stays as built except `order-review`, where the Total reads bigger: label 15/600,
figure at the scale's own money step, **20/700 tabular**. The wallet's 34px stays as built and is
re-examined when that screen is painted.

**Closed 2026-08-03 by the designer:** *"What fills a checked box or a selected radio?"* — **green.**
`--green` behind an `--on-green` glyph, **9.46:1** — the icon system's own rule and the walked node's
exact pair. Both candidates cleared `WCAG 1.4.11`; the budget tension resolves by reading the checked
box as a **status mark** (like the node), not as the screen's one green control, so the primary CTA
keeps that role.

**Closed 2026-08-02, not answered:** *"Does the selected choice card change behind the check?"* — the
choice card turned out to be navigation. Both instances are links that leave the screen, so there is
no selected state for a surface to change behind. The question was asked of the wrong component.

**Closed 2026-08-02 by painting:** *"Does the selected segment invert, or raise a light plate?"* — it
**raises a light plate**, which is what `HIG · Segmented controls` describes: a `--page` plate standing
**1.20:1** above a `--media` track. iOS lifts its plate with a shadow; this one is lifted by tone alone,
because §5 spends exactly one shadow in the system and it is not here.

**Closed 2026-08-02 by painting:** *"How does the 1.21:1 control boundary get to 3:1?"* — it does not
get there, it **goes away**. `concept.md` rev 43 says a surface is a card and not a frame, and rev 45
answered this same 1.21:1 under the form field the same way. A boundary that is absent cannot fail
`WCAG 1.4.11`.

**Closed 2026-08-02, answered by the guideline:** *"Is a lone on/off row a checkbox or a switch?"* —
a **checkbox**, which is what it already is. `HIG · Toggles` reserves the switch for a setting that
takes effect the moment it is flipped; `order-setup`'s "Save this address" sits above a **Continue**
button, so the change is not immediate. The kit carries no switch.

---

## 5. Decisions taken

| Date | Decision | Recorded in |
|---|---|---|
| 2026-08-01 | The selected tab label is `--ink` `#1A1A1A` on the green tint, not `--green-ink` `#3F5B18` — **7.09:1 → 14.75:1**. Fixed on the two `order-history` state pages that had lagged. | `concept.md` rev 42 |
| 2026-08-01 | The focus ring on those two pages keeps `--green-ink` by the designer's call; both values clear WCAG 2.4.11, so it is a choice rather than a survivor. | `concept.md` rev 42 |
| 2026-08-01 | **Flat wins.** §0's CoffeeTech / Fauna "Take" lines and A-1's technique promised a soft shadow; the build has none, and the reason is a stated preference. §0 amended. | `concept.md` rev 42 |
| 2026-08-01 | Six radii are closed for anything the eye reads as an object; **below that the 2 / 4 / 8 scale is permissible** for an internal detail. | `concept.md` rev 42 |
| 2026-08-02 | **A surface is a card, not a frame.** A border does not survive the move into Studio: `--card` at the 1.11:1 tone step, `--r-card` 16, rows separated by their own `--line` hairline. | `concept.md` rev 43 |
| 2026-08-02 | **A selected row carries a checkmark, per HIG** — not a word. `--ink` on `--card` = **14.37:1**, the same glyph the walked node draws. Governs every one-of-many list. | `concept.md` rev 43 |
| 2026-08-02 | A floating surface belongs in a **modal, popover, drawer or dropdown menu**, and there it is appropriate. None exists yet, so `--sh-onphoto` stays unspent. | `concept.md` rev 43 |
| 2026-08-02 | **Imagery goes in wherever an asset already fits.** Fifteen slots on nine screens moved from a text label to a real render — service wells, onboarding slides, the job-brief map. A card well that carries a render grows **56 → 96 × 72**, because below ~96px the rotors stop resolving; the dashed well and its label ground come off, since in the product a cutout sits straight on the card. The remaining slots need pictures that do not exist yet. | `visuals/gaps.md` §A |
| 2026-08-02 | **The brand slot is filled — the mark exists after all.** Reserved in the morning, filled the same day: the designer supplied a quadcopter silhouette from Figma (`56:52`), which is a mark without a wordmark. It goes into the 16 files that hold the slot and nowhere else — the other 68 carry a `‹ Back` + title nav bar, and `HIG · Navigation bars` gives that bar to the screen's own title. Drawn as a mask so it stays inside the grayscale ramp; 60 × 20 at **15.33 : 1**. **Superseded the same day — see the entry below.** | `wireframes/_conventions.md` §8 |
| 2026-08-02 | **The circular action is the form of an icon-only button** — it is the one standing on a painted screen. The square filter on `listings` and the square pencil on `account` are drafts on screens that are neither painted nor hand-edited; what happens to them is decided when those screens are drawn. | `ui/inventory.md` |
| 2026-08-02 | **The client listing card and the operator job card stay two components.** Different jobs, different densities, no forcing one into the other. What *is* worth consolidating is the route — see Open questions. | `ui/inventory.md` |
| 2026-08-02 | **A field has no border** — HIG's own form pattern, confirmed against Apple's own Figma: node `1:301` is the grouped form (no boxes; placeholder, caret, clear button and separator do the identifying) and node `1:424` is the search field (a filled plate). Apple does not choose between the two styles — it uses a row in a form and a plate in search, which is the split this system had already landed on. It lives in a row panel and *is* the row that accepts typing. Value `--ink` on `--card` **14.37:1**, label and placeholder `--slate` **5.95:1**, focus `2.5px --ink` **14.37:1**, error `--danger-ink` **6.14:1**. 1.4.11 governs a boundary that is drawn, not one that is absent — so the failure was the grayscale `--line` border at **1.21:1**, which does not survive painting. The search field is the one exception and keeps a `--media` ground at `--r-input` 10px, which is the iOS search bar. | `concept.md` rev 45 |
| 2026-08-02 | **Shape says how many: a radio is a circle, a checkbox is a square.** Circle = one of a mutually exclusive set; square = an independent on/off, several at a time. `HIG · Radio Buttons / Checkboxes` states exactly this. iOS diverges — it has no radio control and marks one-of-many with a trailing checkmark — but DRON is mobile **web** in a browser with a WCAG 2.1 AA desktop, so the platform convention is what a user reads. **This supersedes rev 43's blanket "a selected row carries a checkmark, governs every one-of-many list":** the checkmark stays for the disclosure row, which is a menu, and gives way to the circle wherever a form asks a question. The defect it names: `payment` and `withdraw` hold `type="radio"` in a **square** box and separate the two only by a 4px bullet against a 4px check, so one shape carries two functions — `WCAG 3.2.4`. **The wireframes are not being changed for it** — recorded here, applied when those screens are painted. | `ui/inventory.md` |
| 2026-08-02 | **An unchecked control's outline is `--slate` `#5A5751`, not `--line`.** The grayscale `1px --line` is **1.21:1** on the card and `WCAG 1.4.11` needs **3:1** for a boundary that identifies a control; `--slate` is **5.95:1**. This is the same 1.21:1 that rev 45 found under the form field — there the answer was to remove the border, because HIG's form row has none; here the box *is* the control and cannot be removed, so it is darkened instead. | `ui/inventory.md` |
| 2026-08-02 | **The choice card is navigation, not a picker** — both instances are `<a href>` leaving the screen. It has no selected state, so it needs a card's set (`--card`, `--r-card` 16, 44pt row, chevron, focus ring) and not a selection treatment. | `ui/inventory.md` |
| 2026-08-02 | **The kit carries no switch, and nothing asks for one.** Counted across all 40 screens: there is no on/off switch in the product. `HIG · Toggles` reserves the switch for a setting that takes effect the moment it is flipped, with no separate Save. The only independent toggle is `order-setup`'s "Save this address for next time", and it sits above a **Continue** button — the change is not immediate, so it is a checkbox, which is what it already is. Recorded as a zero rather than left as a gap. | `ui/inventory.md` |
| 2026-08-02 | **The 2+ rule takes its first two named exceptions.** The segmented control and the in-page tabs stand on one screen each and are in the kit anyway: the rule exists to stop a kit becoming a pile, not to hide a control the system plainly needs. Their counts are shown unrounded as **1 · 0** so the exception stays visible. At the same time the out-of-scope note was **narrowed back to the operator job card**, which is what the designer actually asked to leave out; `.grouptab` and `.segment` were swept in with it by mistake. | `ui/inventory.md` |
| 2026-08-02 | **The in-page tabs take the tab bar's values unchanged, and so does the filter chip.** A row of places with one current is one problem, so it gets one answer: unselected transparent with a `--slate` label **6.62:1**, selected `--green-wash` with an `--ink` label **14.75:1**, `--r-pill`, 32px visible with the hit area extended to 44 by a transparent `::after` — which is what iOS does with its own 32pt controls. The fill step is only **1.085:1**; the ink change is the second signal, and `concept.md` already records that pairing as the answer here. Three selection patterns became one. | `ui/inventory.md` |
| 2026-08-02 | **The segmented control raises a plate and loses its border.** Track `--media` at `--r-btn` 12 reading **1.20:1** below the page; selected a `--page` plate inset 2px at `--r-input` 10, standing **1.20:1** above the track — the largest tone step the palette holds, and the ramp's own arithmetic, 12 − 2 = 10. Label `--ink` **15.99:1** selected, `--slate` **5.51:1** at rest. No border and no dividers: the 1.21:1 boundary is removed rather than darkened, per rev 43 and rev 45, and a boundary that is absent cannot fail 1.4.11. **No green** — this is card UI, and §5 does not spend the accent inside a card. | `ui/inventory.md` |
| 2026-08-03 | **Five designer calls close the Client leftovers.** (1) **A filled rating star is yellow** — the palette's own `--warn`, drawn nowhere until now, gets its first home; an unfilled star is a grey glyph with no outline. Named number: `--warn` on the page is **1.84:1** against 1.4.11's 3:1, so state is never colour alone — the fill pairs with `aria-pressed` and the whole button is the 44pt target. The star is the one glyph that fills instead of stroking: a rating is a quantity, not an icon. (2) **The applied filter chip sits on the green tint with no boundary**, its ✕ a solid-green pill with a charcoal cross (9.46:1) — and the `Price:` prefix comes off the chip's wording. (3) **Support speaks as DRON**: the agent card carries the DRON mark, not initials. (4) **`share` becomes a bottom sheet** — the system's first floating surface, exactly where rev 43 said one belongs, so `--sh-raised` is finally spent and the scrim is warm ink at 40%; Copy link is the primary, the three channels are icon buttons, the share glyph tops the milestone. (5) **The address icon slots go**, and the two address labels take a colon. | `ui/kit.css`, `wireframes/*` |
| 2026-08-03 | **The Client role wears the kit — 36 screens dressed in place.** Every Client screen with its states (`listings` ×6, `order-setup` ×3, `time-slot` ×2, `order-review` ×2, `payment` ×3, `order-confirmed` ×4, `rate`, `share` ×2, `inspection-report` ×3, `support`, `report-issue` ×3, `resolution`, `contact-support` ×2, `account`, `account-edit`, `switch-role`) links `ui/kit.css` and draws through `dr-*` classes; structure and copy stay from the frames. **Forms fold into row panels** per rev 45; **payment's squares became green circles** per revs 46–47; the tab-bar, status-bar, search, filter, pencil, select and chevron **glyphs went into their slots** — 161 `.sq` slots reduced to 4. Verified across all 36 in the browser: every frame renders, zero horizontal overflows. **Honest leftovers, kept grayscale on purpose:** the rating stars (`rate`), the dismissible filter chips (`listings-filtered`), the client's initials avatar (`account`, `contact-support`, `order-confirmed-empty`), the 34px amount (`payment` — the designer's "leave as is"), three text milestones (`share` SHARE, `switch-role` SWITCH, `payment-loading`), two address-field icon slots (`order-setup`), and the label media wells still waiting on images. | `wireframes/*`, `ui/kit.css` |
| 2026-08-03 | **The Total on `order-review` reads bigger; nothing else moves.** Label 15/600, figure **20/700 tabular** — the scale's own money step, no eleventh size. Both places that state the fact take the same size (voice.md P3: the price is a fact before the tap). The wallet's 34px stays as built. The one wireframe touched is `order-review.html`, at the designer's explicit instruction. | `wireframes/order-review.html` |
| 2026-08-03 | **The pager's active dot is green.** Designer's call: active `--green`, resting dots plain `--btn2` discs with no outline. Green on the page is **1.69:1** against 1.4.11's 3:1 — accepted on the live-disc precedent: never the sole carrier, each dot keeps its accessible name. | `ui/kit.css`, `ui/kit.html` |
| 2026-08-03 | **Checked is green.** The designer's call: a checked box or selected radio is a `--green` fill behind an `--on-green` glyph — **9.46:1**, the walked node's exact pair and the icon rule's own "active / done is a green shape behind a charcoal glyph". The kit's first draft had charcoal; corrected in `kit.css`, the showcase and the artifact. Budget reading: a checked box counts as a status mark, not as the screen's one green control. | `ui/kit.css`, `ui/kit.html` |
| 2026-08-12 | **Only the current mode carries the mark, and the mark is green.** Designer's call on `account`'s mode picker: the built page marked *both* rows with a charcoal checkmark — now only the current role (Client) is marked, and the Operator row answers with hover alone (`--media`, the secondary button's own step). "A green primary checkmark" lands per the fill-only rule as the **done node** — a green disc behind a charcoal glyph, **9.46:1** — never a green glyph, which is 1.52:1 on the card and would be the state's only indicator against 1.4.11's 3:1: the same conversion the tab bar and the checked box already made. The state is programmatic too — `aria-current="true"` on the row (4.1.2) — and the item rows gain the kit's paired focus ring, drawn inside because the card clips at its radius. | `wireframes/account.html`, `ui/kit.css`, `ui/kit.html` |
| 2026-08-12 | **A panel of fields gives up its side padding — the field carries the 16 itself.** Designer's flag on `rate`'s review box: margins wrong, "doesn't look like a text area". Measured: `.dr-field` (a full-bleed row, pad 16) inside `.dr-rows` (pad 16) put typed text at **32px** where every other line on the card stands at 16 — on `rate`, `contact-support`, `report-issue` and `order-setup` alike, so the fix is the kit's: `.dr-rows:has(> .dr-field)` drops horizontal padding, and the between-field hairline now runs indent-16-to-the-right-edge, iOS grouped-list fashion. The area also takes the body rhythm (`--t-para-line`) instead of UA `normal`. And `rate` was the one compose card with **no visible label** — placeholder-only, against `HIG · Text fields` (a placeholder is not a label: it vanishes on typing). It takes **"Your review"** on the label's own line, the `contact-support` pattern; string added to `microcopy.md` first. | `ui/kit.css`, `wireframes/rate.html`, `microcopy.md` |
| 2026-08-12 | **Clear all on the filtered list is the screen's green control.** Designer's call: the tertiary read as plain text, and she wants a green button. Green as *text* is barred twice over — the fill-only rule, and **1.69:1** on the page against 1.4.3's 4.5:1 — so "green button" lands as the existing primary: charcoal on green, **9.46:1**. Budget, computed not asserted: solid green on the frame becomes the three ✕ pills (972 px²) + the button (4 184 px²) = **1.7%** of the 375-frame against the ~5% ceiling, with the button at **81%** of the screen's green — the documented envelope's own lower bound ("the CTA is 81–100%"). The one-green-control slot was free: no other CTA exists on the screen, and the family already set the precedent — `listings-empty`'s **Clear filters** is a green primary. | `wireframes/listings-filtered.html` |
| 2026-08-12 | **`rate` composes on the axis: the stars are centred, the review takes half the screen.** Designer's feedback on the built page: the star row hugged the left edge and the review was an 80px strip. The row gets `justify-content: center` — layout moves, nothing shrinks: five 44pt targets and the 8px gaps are unchanged (`HIG · 44pt target` ✓, `WCAG 2.5.8` ✓; alignment has no criterion, both placements conform). The review field takes a new kit modifier **`.dr-field--half`**: the textarea's floor is `--screen-h`/2 less the label line (21px), the row gap (10) and the two 12px paddings, so the visible row lands at **406px — half the 812 frame**. A floor and never a height, per the kit's own rule, and the floor rides on the *input*, not the row — a min-height on the wrapped row would be dealt out across the label's flex line by align-content. `--half` is spent on `rate` alone; the 80px `--sz-area-min` floor still governs `order-setup`, `report-issue` and `contact-support`, whose areas sit among other rows. **Measured on the headless build:** the star cluster's centre sits on the frame axis to the pixel (both 459 in the same capture), and the row decomposes 12 + 21 + 10 + 351 + 12 = 406. One consequence, seen not asserted: with the *annotation* labels shown the main column runs ~25px past its window and scrolls; with them hidden — the product view — the screen composes with ~40px of slack and no scroll. Annotation chrome, not product; recorded, not fixed. The display line "Rate this order" stays left-aligned — the feedback named the star row. | `ui/kit.css`, `wireframes/rate.html` |
| 2026-08-12 | **`rate` reads as two zones, and both lines of its copy are the designer's.** Second pass on the screen, three calls. (1) The rating zone stacks on `.dr-center` — the heading centres over the centred stars with the stack's own 12 between them. (2) Zones separate by proximity, not by a divider: a new `.dr-main--zones` modifier lifts the screen's section gap 16 → `--sp-24`, so the rhythm is 12 *within* a zone against 24 *between* zones — a 2:1 step where before both read 16/0. Spent on `rate` alone; every other screen keeps the 16. (3) Copy: the heading becomes **"How would you rate this order?"** — the designer's own sentence, question mark added, and a recorded *reversal* of the Batch-2 rewrite that had turned "How was it?" into "Rate this order": her call wins, and the new line is a specific question naming the object, not the banned cute prompt. The review label lands as **"Share your review of this order"** — her sentence shape, with *feedback* → *review* because the same screen's CTA is "Submit review" and A9 keeps one word per concept. Both strings went to `microcopy.md` first (rewrite log + master). | `microcopy.md`, `ui/kit.css`, `wireframes/rate.html` |
| 2026-08-13 | **The question takes two balanced lines, and the zone pays for them from its own pocket.** Designer's flag: the one-line heading ran edge to edge (measured 340.8 of the 341 the panel gives). It wraps by a *measure*, not a `<br>` — `.dr-display--balance` caps the line at a new `--t-display-measure: 18ch` with `text-wrap: balance`, so the UA string will re-balance itself and no markup hard-codes the break. The second line costs 30.8px against a frame that held ~0.4px of slack — a screen that scrolls by a hair is a defect — so the rating stack closes 12 → 8 (`.dr-center--tight`, the kit's own `--sp-within-lg`): the proximity step between zones sharpens to **24 against 8, 3:1**, and the column sums 61.6 + 8 + 44 + 24 + 406 = **543.6 in the 548 the frame gives** — no scroll, ~4px of slack. Copy unchanged, so `microcopy.md` does not move. | `ui/kit.css`, `wireframes/rate.html` |
| 2026-08-12 | **Clear all takes the green text button — `.dr-btn--text`, the rung rev 48 cut.** The designer's confirmation the preview was waiting on arrived: a text button in the primary green. The raw hex cannot be text (**1.69:1** on the page against `WCAG 1.4.3`'s 4.5:1), so the button rests in `--green-ink #4F6B22` — **5.58:1** on the page, **5.01:1** on the card; hover and press darken to `--ink`, the tertiary's own grammar; `min-height` keeps the **44pt** target (`HIG · 44pt target` ✓, `WCAG 2.5.8` ✓); disabled and the focus ring are inherited from `.dr-btn`. This closes the control's day-long walk — green primary (morning) → slate tertiary (afternoon) → green text (standing) — each turn logged in this table. Green budget untouched: an ink is not a fill, the frame's solid green stays the ✕ pills and the count badge, and the one-green-control slot stays empty. The variant enters the kit and the showcase; `DESIGN.md`'s palette note, contrast table, buttons and don'ts are corrected to carry it. | `ui/kit.css`, `ui/kit.html`, `wireframes/listings-filtered.html`, `DESIGN.md`, `concept/concept.md` rev 49 |
| 2026-08-12 | **Clear all goes back to the text action — the same day's green primary is reversed.** Designer's feedback on the built page: the button should be the text one. This is the morning's call (the row below) overturned on her word, and it is logged so the ledger shows the green primary as *tried and reverted* — not to be re-proposed. Conformance, both states pass, so this is taste not correction: the tertiary's label is `--slate` on the page, **6.62:1** against `WCAG 1.4.3`'s 4.5:1 (hover takes `--ink`, 15.99:1), and `.dr-btn`'s `min-height` keeps the **44pt** target (`HIG · 44pt target` ✓, `WCAG 2.5.8` ✓). Budget after: solid green on the frame is the three ✕ pills alone, **972 px² ≈ 0.3%**, and the one-green-control slot stands empty — permitted, the ✕ pills are status marks, and `support` now carries the family's green where a screen has a CTA to spend it on. The known consequence is the one the morning named: beside the outlined chips the action reads as plain text — accepted this time as the intent. | `wireframes/listings-filtered.html` |
| 2026-08-12 | **Contact support on the support hub is the screen's green control.** Designer's call: primary, not secondary. Conformance: the screen held no green at all, so the one-green-control slot was free; charcoal on green **9.46:1**; the block CTA is **4.9%** of the 375-frame — inside the measured envelope (the painted pages run 4.43–5.09%). It also matches the voice rule the button already carries: *Contact support* is the one named way to reach a human, and the screen's job — "can't find your issue" — ends in exactly that action. | `wireframes/support.html` |
| 2026-08-12 | **The mode mark loses its disc: a bare green checkmark in `--green-ink`.** The designer read the green disc as a control — *"is this a checkbox or a radio? Neither — just a green checkmark, no background"* — and she is right by the system's own book: rev 46 gave circles and squares to *forms*, and the disclosure row is a menu of routes whose mark is iOS's current-item checkmark. This supersedes the morning's done-node reading. A lone glyph is the state's sole indicator (`WCAG 1.4.11`, 3:1) and signal green fails it at **1.52:1** — so the palette gains its missing rung, **`--green-ink #4F6B22`** (rev 48): the same hue at the text-safe step, **5.01:1** on the card, **5.58:1** on the page, completing the `warn/warn-ink · danger/danger-ink` pair grammar. First spend: this checkmark. `--green` stays fill-only. The same token is what the pending text-button preview proposes — one rung, two homes, her confirmation still open on the button. | `ui/kit.css`, `wireframes/account.html`, `ui/kit.html`, `DESIGN.md`, `concept.md` rev 48 |
| 2026-08-12 | **The mode checkmark is Signal Green — an accepted exception, recorded not corrected.** The designer saw the `--green-ink` mark and named the brand hex: `#9BCF4A`. On the card that is **1.52:1** against `WCAG 1.4.11`'s 3:1, and the call was made with that number stated — so it enters `DESIGN.md`'s below-3:1 table (rev 50) beside the live disc (1.52:1) and the pager dot (1.69:1), on the same stated reason: colour does not carry the state — the mark's **presence** does (one row a checkmark, the others nothing), and `aria-current="true"` speaks it to assistive tech. The fill-only rule now reads: never text, never a stroke, **one recorded exception**. `--green-ink` keeps the text button as its one home. | `ui/kit.css`, `ui/kit.html`, `DESIGN.md`, `concept.md` rev 50 |
| 2026-08-13 | **The applied row opens with All; Clear all comes off.** Designer's call on the built page: delete the Clear all button, add an "All" chip first instead. The exit to the full list moves from a text action into the row itself — the leading chip — so every exit in the row is now a chip. New kit modifier **`.dr-fchip--all`**: the fchip pill on the secondary button's ground (`--ink` on `--btn2`, **11.54:1**, `WCAG 1.4.3` ✓) — an exit is an action, not an applied filter, so it does not wear the applied `--green-wash`; no ✕, symmetric 12px padding, floor **28px** (the sibling's 18px ✕ + 2×5). Targets: 28px clears `WCAG 2.5.8`'s 24px AA floor and sits under `HIG`'s 44pt exactly as the built row's chips do — recorded, with the grouptab's `--pill-bleed` extension to 44 as the open option. Consequence: `.dr-btn--text` (rev 49) loses its only spend and stays a declared variant. Strings went to `microcopy.md` first; `_screens.md`, `_conventions.md`, `flows.md` and `DESIGN.md` corrected in the same pass. | `microcopy.md`, `wireframes/_screens.md`, `wireframes/_conventions.md`, `flows.md`, `ui/kit.css`, `wireframes/listings-filtered.html`, `DESIGN.md`, `concept/concept.md` rev 51 |
| 2026-08-13 | **The All chip carries the ✕ like its siblings.** Designer's feedback on the built row — *add the close icon the chips already have to the cancel-all chip* — a part-reversal of the row above's "no ✕ — nothing to remove", logged so the ledger holds both. The ✕ is the sibling's `.dr-fchip__x` unchanged: `--green` pill, `--on-green` cross, **9.46:1** (`WCAG 1.4.3` ✓); the ground stays `--btn2` (**11.54:1**) — an exit is an action, not an applied filter. New pair measured before use: the pill's edge on `--btn2` is **1.22:1** beside **1.56:1** on the siblings' wash — neither ground makes the edge the carrier, the cross at 9.46:1 does, the component's settled reading. `.dr-fchip--all` reduces to the ground swap: the padding override and the `min-height` floor come off, the 18px pill re-establishes the **28px** stand (`WCAG 2.5.8` ✓ ≥ 24px; under `HIG`'s 44pt — the row's recorded departure, unchanged). Budget: the All ✕ adds **324 px²**; measured on the build after the same-day sort-chip removal landed in parallel, the frame's solid green is three ✕ pills + the count badge = **1 296 px² ≈ 0.4%**, status marks all; the one-green-control slot stays empty. Copy does not move — the ✕ is `aria-hidden` and the label already says *All — clear applied filters*. | `concept/concept.md` rev 52, `ui/kit.css`, `wireframes/listings-filtered.html` |
| 2026-08-13 | **The action-bar Total line comes off: the bar is the button.** Designer's feedback on the built `order-review`: *remove the ₴180 total from the bottom bar throughout the project.* Three bars carried it and all three drop their `.dr-price` line — `order-review` (`Total · ₴180` above `Pay ₴180`), its loading state (`Total · …` above the transient `Continue`), and `order-setup` (`Total · locked, no extra fees · ₴180` above `Continue to review`). This narrows 2026-08-03's "in both places the fact appears" to **one place**: the breakdown's last row keeps the sized Total, and the CTA's own verb (`Pay ₴180`) still names the amount on the tap. Voice **P3 holds where the commitment is**: on `order-review` the full locked price stands in the breakdown and on the button before Pay. The named consequence: **`order-setup` now states no price at all** — the locked number arrives one tap later, on the review screen, which is the core flow's own "See price" step (Choose service → Enter details → **See price** → Pay). Conformance: the line was static text restating a fact stated elsewhere, not a control — no `WCAG 1.4.3` pair is lost that carried unique information, no `HIG` pattern asks a bar to repeat its button's amount, and the bar reduces to the block CTA the action-bar spec already describes ("full-width buttons"). **Boundary cases left as built, on purpose:** `payment`'s bar line (`Pay with Apple Pay · ₴180`) and `time-slot`'s (`Selected · Wed 2 Jul, 09:00 · ₴800` / `No slot selected`) are a method line and a selection summary, not totals — whether they follow is the designer's word, not this sweep's. `.dr-price--total` stays in the kit declared and spent nowhere. Strings came off `microcopy.md` first. | `microcopy.md`, `wireframes/order-review.html`, `-loading`, `wireframes/order-setup.html`, `ui/kit.css`, `concept/concept.md` rev 53 |
| 2026-08-13 | **The rate compose card is one surface.** Designer's feedback on the built page: *the field shouldn't contain one more text area inside — one area, one helper sentence.* Measured as built: the row's card (`--card`, r16) held the area's own `--media` ground (r10) within it — two nested surfaces, and two lines of copy above the typing space (label + placeholder). The decision went to `microcopy.md` first: the card IS the area. New kit modifier **`.dr-field--bare`** strips the input's ground, radius and padding — zero painted grounds remain inside the card; the visible label comes off to `.sr-only` **"Your review"**, and the placeholder is the one sentence of help text, *What the operator did well, and anything to improve*. Conformance: this knowingly departs from `HIG · Text fields`' "a placeholder is not a label" — the objection that added the visible line on 2026-08-12 — on the pattern HIG itself ships in the App Store's review compose (stars, then a placeholder-only text view), and the name survives programmatically: `WCAG 4.1.2` ✓ via the sr-only label; hint `--slate` on the card **5.95:1**, typed ink **14.37:1** (`WCAG 1.4.3` ✓ both — the row panel's own recorded pairs). Recorded, not corrected. Geometry: the sr-only label leaves the flex flow, so `--half`'s floor takes back the label line and the row gap — the row still lands at **406 = 12 + 382 + 12** (headless build; the designer's browser measured 405.93 before the change), the card at 410: the frame's composition does not move by a pixel. Spent on `rate` alone; the grounded area (2026-08-12) still governs `order-setup`, `report-issue` and `contact-support`, whose areas sit among rows. | `microcopy.md`, `ui/kit.css`, `wireframes/rate.html` |
| 2026-08-15 | **The order card reads name-first: the header row reverses.** Designer's feedback on the built `order-history`: *in these cards change the place for title and subtitle in the left and image with status in right side.* **As built**, the 96 × 72 drone well led the header and the name sat to its right; now the name over date/time leads at the card's own **20** inset and the well closes the row at **20**, its status mark riding the top-right corner at **26** from the card's top and right edges — measured, unclipped. **Done in the DOM, not with `flex-direction: row-reverse`:** a visual reversal that leaves the source order behind is the classic `WCAG 1.3.2 Meaningful Sequence` failure, and the swap also improves what a screen reader says — *"Package delivery · Today 10:03 · In progress"* instead of the state before the thing it belongs to. **No rule changed**, only the order: `.dr-order__head` already carried `flex:1` and `.dr-well` `flex:none`, so both work from either end, and the 389px tier only narrows the gap. **The skeleton flips with it**, which is the point — `order-history-loading` measures **263.0** against the delivered card's **263.6**, the same half-pixel the no-reflow contract has always stood at, and the live card holds **376.06** against the **375.99** the designer's own browser recorded before the change. Nothing else on the card moves: rail, From ▸ To inset, fee row and actions are untouched, and no string moves, so `microcopy.md` is not involved. | `wireframes/order-history.html`, `wireframes/order-history-loading.html`, `concept/concept.md` rev 63 |
| 2026-08-15 | **Price comes back to the filter sheet as a band, and it is not the sort that was removed.** Designer's feedback on `listings-filters`: *add prices low medium high, but in front of high set the badge insurance* — then *do variant 2 and add a horizontal line with bullets you can drag, and the price changes there.* **First, the thing worth not re-litigating:** `_screens.md` records a Price control deleted 2026-08-13 — *Recommended / Low→High / High→Low* — but that was **an ordering**, which rearranges the list and removes nothing; a band **narrows** it. Different control, different job; the sort stays deleted and `_screens.md` now says so in as many words. **Two departures put to her with the standards first; she took the literal reading of her brief in both, and they are logged as decisions rather than filed as defects.** (1) `voice.md` **P2** — *show the proof, not the adjective* — against `Low / Medium / High`: three adjectives, no figure, on a screen whose cards show **no price at all** since rev 53 moved the locked number to `order-review`. The zone answers it in the control she asked for rather than in the labels: the range's readout states the band as a fact, **₴200 – ₴500**, tabular, on every drag. (2) The **Insured** chip on `High` — `.dr-chip--trust` (rev 58) sits on the **operator card** on `order-confirmed` and `order-confirmed-empty`, on the 214-job veteran and the no-review newcomer alike, so in the built product *Insured* is true of every operator and a badge on the dearest band alone reads as *the other two are not*. Named before it was built. **New component `.dr-range`** — see its row above for the measured pairs and targets. **Green budget, measured and reported, not inherited quietly:** the frame stands at **5.69%** solid green (`Apply filters` 15,022 px² + shell `EN` 1,152 + two checked radios 1,152) against the ~5% ceiling — **the new zone adds 0 px²**, so the overage predates this pass; counting only the product frame it is **5.31%**. Left as found: the only way down is the primary button or the language switch, and neither belongs to this feedback. Strings went to `microcopy.md` first, `_screens.md` before the HTML. **Not swept, on purpose:** `listings-filtered` draws a chip per applied filter and now has a dimension with no chip — hers to say. | `microcopy.md`, `wireframes/_screens.md`, `ui/kit.css`, `wireframes/listings-filters.html`, `concept/concept.md` rev 66 |
| 2026-08-15 | **Every area gives up its ground — the card is the surface, on all four compose screens.** Designer's feedback on `rate`: *remove additional background on the text areas.* First finding, reported before anything moved: **`rate` was already bare** — the 2026-08-13 pass above did it, and her browser was holding the build from before it (her snapshot read `background rgb(228,225,218)`, `padding 12px`, height **351**, which is `--half` *without* the sr-only floor — the pre-change row exactly). So the live question was the other three, and her call was to strip them: `order-setup`, `report-issue`, `contact-support`. **What came off:** the `--media` well at `--r-input` 10 with its 12px inset, added 2026-08-12 and now three days old. **`WCAG 1.4.11` loses nothing** — measured, the well stood at **1.08:1** against the card, nowhere near the 3:1 a boundary needs to be an identifier, so it was affordance and never the carrier; the identifier stays the label + placeholder + focus ring, and each of the three keeps its **visible** label, so 2026-08-13's recorded departure from `HIG · Text fields` (placeholder-as-label) is `rate`'s alone and does not spread. **Both pairs improve** by moving off the well onto the card: placeholder `--slate` **5.51:1 → 5.95:1**, typed `--ink` **13.33:1 → 14.37:1** (`WCAG 1.4.3` ✓ both). Geometry, measured on the build: typed text moves **28 → 16px** from the card edge, level with its own label at last — the same double-inset fault 2026-08-12 fixed for the row, now fixed for the input; rows stand unchanged at **111 = 12 + 21 + 10 + 56 + 12**, all four frames hold **812** and none overflows. **`rate` does not move by a pixel: 382 / 406 / 410**, the figures the entry above recorded. Kit consequence: **`.dr-field--bare` retires** — when every area is bare the modifier names no difference — and the half-screen floor re-keys to **`:has(> .sr-only)`**, which names its real cause (the label that is spoken and not drawn); `.dr-rows:has(> .dr-field)` is the precedent for `:has()` in this kit. `--media` at `--r-input` goes back to being the **search bar's alone**, where 2026-08-02 put it (verified on `listings`: `#E4E1DA`, r10, 44px). No string moves — every label and placeholder stands as written, so `microcopy.md` is untouched. | `ui/kit.css`, `wireframes/rate.html`, `concept/concept.md` rev 56 |
| 2026-08-15 | **The document zone gets a name, and the card stops holding a box.** Designer's feedback on the built `account-edit`, in two calls. **(a) The heading.** *«зроби окремо заголовок над зонами загрузки: "Documents"»*, and — asked which of the two readings her closing line meant — *«зроби один заголовок Documents для обох карток»*: **one** `Documents` over the pair, not one per card. The zone had no product name at all, because `.dr-label` is the wireframe's annotation and vanishes with the shell's toggle, which is how she reads the page. New kit rung **`.dr-sect`**, SF 13/600 `--slate` on `--page` — **6.62:1** (`WCAG 1.4.3` ✓), an `<h2>` so the grouping is programmatic (`WCAG 1.3.1` ✓) where the `<span>` label it replaces carried none. She was shown that insurance is also a document and chose the shared heading anyway: **her call, recorded as a decision, not a defect.** **(b) The ground.** *«в полі загрузки прибери додатковий бекграунт … як title "Add drone documents" and as subtitle "Regisration serial etc."»* — the nested `--media` well goes and the card becomes the upload surface, carrying glyph, title and subtitle. Measured, that well was **1.08:1** on the card: affordance, never the identifier (`WCAG 1.4.11` loses nothing — the glyph at 5.95:1, the title's verb and the `--ink` focus ring at 14.37:1 are what identify it, and the ring is rev 57's, still applying). **Both pairs improve by moving onto the card:** title `--ink` **13.33 → 14.37:1**, glyph and subtitle `--slate` **5.51 → 5.95:1**. **Target grows:** a 294 × 140 box inside a 326-wide card becomes the whole card, **326 × 161** and **326 × 143** (`HIG · 44pt` ✓, `WCAG 2.5.8` ✓). **Name and description, by construction:** `aria-labelledby` → the title, `aria-describedby` → the subtitle, so the name is exactly the visible label (`2.5.3` ✓, `4.1.2` ✓). **Two in-card labels retire** — `Drone documents` and `Insurance` — their job split between the heading above and the title inside; `microcopy.md` first, as always. **Rhythm, measured with annotations off:** Personal card → heading **24** (the kit's zone gap), heading → card **12**, card → card **12**, and the form's scroll shortens **822 → 755**. **One consequence, reported and not fixed:** the group closes on **12**, the same gap it uses inside, because `Personal` and `Preferences` have no heading of their own to open with — whether they get one is her call. `.dr-rows--center` loses its only user and goes **on the bench**, declared and spent nowhere; it is kept, not retired, because it still names a real shape. | `microcopy.md`, `ui/kit.css`, `wireframes/account-edit.html`, `concept/concept.md` rev 60 |
| 2026-08-15 | **The upload zone stops holding a button and becomes one.** Designer's feedback on the built `account-edit`, beside a Figma reference showing a glyph over a single line: *improve upload area — write just with text.* **As built:** each document card held a centred `.dr-upload` with an ordinary `.dr-btn--secondary` under the 32px tray glyph — a pill measuring **1.25:1** against the card it sat on, whose edge therefore said nothing, so the words were already doing the identifying on their own. **Now:** the pill comes off, the words stay as `.dr-upload__action` at the button's own **15/600**, and the **zone takes `role="button"`** — the shape `report-issue`'s evidence well has carried since 2026-08-03 and the one the standing kit rule was written for (*only where the zone is itself the control does the `--media` fill stay*). This is the build half of the text-action call `microcopy.md` recorded on **2026-08-13** and never drew; **no string moves** — `Add drone documents` / `Add insurance` stand as that day settled them. **Conformance, measured first:** the tap target grows **44 → 140px** on its short side (`HIG · 44pt` ✓, `WCAG 2.5.8` ✓ against 24); the label lands `--ink` on `--media` at **13.33:1** and the glyph at **5.51:1** (`WCAG 1.4.3` ✓, `1.4.11` ✓); the well's own **1.08:1** against the card is **not claimed as the identifier** — same reading as the areas above, kept here because an upload zone has no label-value anatomy to fall back on. **Two kit gaps closed by the role change:** `.dr-upload[role="button"]` had **no focus ring** (`WCAG 2.4.7` failing on `report-issue` since 2026-08-03) and no hover — it now takes the universal `2.5px --ink` ring at **13.33:1** and a `--media` → `--btn2` hover, both of which land on `report-issue` as a fix and change nothing there at rest. **The Figma's dashed frame is not re-drawn** — `--line` on `--card` is **1.21:1** against `1.4.11`'s 3:1, removed at rev 45 and re-affirmed at rev 56; **it can conform in `--slate` at 5.95:1**, held for the designer's word. **One defect the change created, caught by measuring:** `.dr-upload` is a flex item and was always shrink-to-fit — invisible while both zones held the same `Add document` (about 185 each), ragged the moment each took its own object, measured **214 against 156** on a 332px card. `.dr-rows--center .dr-upload` now takes `flex-basis:100%`: both are **300 × 140**, standing at the row's own 16px inset each side. Scoped to the centred document card — that rule's own 2026-08-12 context — so `report-issue`'s zone is not resized. **Found beside it, reported not swept:** both hints are direct children of `.dr-rows` rather than `.dr-field`, so they miss the row's `0 16px` inset — the wrapped drone-documents hint measures **2px** from the card edge against the 16 every other line stands at. | `microcopy.md`, `ui/kit.css`, `wireframes/account-edit.html`, `concept/concept.md` rev 57 |
| 2026-08-15 | **The palette gains its first cool colour, and the Insured badge is promoted from fact to confirmed fact.** Two designer calls on the built `order-confirmed-empty`. **(a)** *remove this sentence* — the reassurance line *"Every operator is licence-checked and insured by DRON before their first job — reviews build from here."* comes off. The screen loses nothing it carried: `voice.md`'s empty pair is still answered on the frame (**why** = *New operator · no reviews yet*, **way out** = *Track anyway* / *Ask for another operator*), and `flows.md` RJ-C1's "platform-backed reassurance" moves from prose to the two badges — **P2**, show the proof not the adjective. **(b)** *«зроби бейдж голубим кольором і справа іконка галочка»* — new pair **`--trust-wash #D8E6F2` / `--trust-ink #2A5C7D`**, measured before it was drawn: **5.65:1**, which is not a free choice but the family's own rung, the figure `--warn-ink` on `--warn-wash` already stands at (`--danger-ink` 5.49 beside it). `WCAG 1.4.3` ✓ for a 12/600 chip. Wash **1.05:1** on the card, inside the band green 1.03 / warn 1.01 / danger 1.12. Only the two used rungs are declared — no solid `--trust`, because `--danger` has sat declared-and-undrawn since rev 25 as the reason not to. **Not a second accent:** the palette comment already said *one accent, two semantic washes*; this is the third wash in the pair grammar `warn` and `danger` established, and Signal Green stays the one accent and stays fill-only. **Anti-references checked and none bars blue** — `PRODUCT.md`'s four are OLX/classifieds, DJI-hobbyist orange, SaaS cream and *the exact Bolt/Glovo colour language*; the only "no blue" is rev 6's description of the **direction** round as reflex-dodging, already overtaken on its own terms since **green was on that same list and became the accent**. Recorded, not stepped over. Checkmark: the one icon system, 24-grid at 1.7, `currentColor`, rendering **0.99px** in the 14px chip box (inside 0.85–2.27), sitting **after** the word per her *справа*, `aria-hidden` because the word carries it (`1.4.1` ✓ — the mark is never the sole carrier). **`order-confirmed` moves with `-empty`** because `_conventions.md` §12 requires siblings to hold one DOM structure; measured identical on both. **Open:** whether the three tracking pages follow, and whether *confirmed* should be **said** (`Insurance confirmed`) rather than only drawn. | `microcopy.md`, `DESIGN.md`, `ui/kit.css`, `wireframes/order-confirmed.html`, `-empty`, `concept/concept.md` rev 58 |
| 2026-08-15 | **The load error names its cause: alert triangle → wi-fi fan on `listings-error`.** Designer's call on the built page — *change icon on wi-fi signal icon.* **Logged as an overturn, not an overwrite:** rev 54 put that triangle there two days ago on her own *«Add alert icon»*. The triangle said *something went wrong*, which the title already says; the body says **"No internet connection. Check it and try again."**, and the glyph now says what the sentence says. **No new geometry** — the three arcs are copied byte-for-byte from `tracking-error`'s mark, so §0.2's States row gains a second *state*, not a second *glyph*. **Two written rules settled the treatment before it was built.** §0.2 bars the slash on this glyph in as many words (*No slash — that reads as disabled*), so the wi-fi-off idiom is not drawn even though the kit owns a slash elsewhere (`delivery-error`, `M4 4l16 16`); and §0.2 reserves opacity to one state alone, so this fan is **not** dimmed. The difference between the two connection states is therefore the ink's strength and it means something: dimmed = a live signal that stopped, full = a connection the screen is naming. **Measured:** `--danger-ink` on `--danger-wash` at full strength **5.49:1** (`WCAG 1.4.11` ✓ ×1.83 against 3:1); box unchanged at 26px so the 1.7 stroke renders **1.84**, inside the set's 0.85–2.27. Mark stays `aria-hidden` with `role="alert"` beside it (`1.1.1`, `1.4.1` ✓). No string moves. **Found beside it, reported not swept:** `tracking-error`'s dimmed fan measures **2.43:1** on its wash, under `1.4.11`'s 3:1 — a departure §0.2 grants deliberately and nothing rests on the glyph alone, but the number is now on the books; `.7` would measure 3.21:1 and `.8` 3.85:1 if it should conform instead. | `wireframes/listings-error.html`, `concept/concept.md` §0.2 + rev 62 |
| 2026-08-15 | **Tracking swaps the vertical track for the kit's horizontal rail, under the map.** Designer's call on the built `tracking` — *«зроби цей прогрес бар … горизонтально розмісти під зображенням з картою»*. No new component: `.dr-rail` has existed since the `order-history` pass and its `--at2` modifier is exactly this state (walked 1 → 2 of 4), so the screen adopts it instead of growing a second progress form. Measured: the vertical `.dr-track` stood **142.0px**, the rail stands **45.7px** — **96.3px** returned, and the block moves from last-on-screen (below the fold) into the first viewport, which is what the instruction was for (RJ-C2). Fill **81.5px** of a **244.5px** span = exactly ⅓ ✓; labels one line each in an 81.5px column, no wrap at 375. Applied to `tracking`, `-empty` and `-error` — one screen may not hold two layouts; `-loading` has no progress block. **Cost recorded:** the label drops 14px → **10.5px** (`--t-microsm-size`), under `HIG`'s 11pt floor — not a new departure (`order-history` has shipped it since 2026-08-03) but now on a second screen, hers to confirm. Contrast fine either way: slate **6.62:1**, current step ink **15.99:1**. Kept as a `<ul>/<li>` so the milestones stay a list (`WCAG 1.3.1`), where order-history uses spans; no kit edit needed, `.dr-rail__step` is `display:flex` so no marker renders. **Consequence:** `.dr-track` now has no product user and joins the bench of declared-and-unspent rungs — left declared, since a longer or variable-length sequence still wants the vertical form. | `wireframes/tracking.html`, `-empty`, `-error`, `concept/concept.md` rev 65 |
| 2026-08-15 | **The route strip's skeleton collapses to one bar — and the card does not move by a pixel.** Designer's call on the built `order-history-loading`: *«прибери усі блоки з цього прямокутника лише один прямокутник»*. **As built:** each `.dr-inset` held **five** pulsing pieces — two `.dr-sk-stop` columns (label bar over place bar) with the 26px `.dr-sk-dir` chip between — rehearsing far more detail than the loaded strip rewards, which is two short place names. **Now:** one `.dr-sk-route` spanning the content box, **260 × 35**. **The height is the point and it is not a magic number:** the column it replaces measured `--sk-lbl-h` 12 + `--sk-stop-gap` 7 + `--sk-place-h` 16 = **35**, and the bar is declared as that sum, so it follows those three if they move. **No-reflow contract verified at both tiers, before and after:** inset **67**, card **263**, list **845**, main **619** at 390px; inset **63**, card **227** at 375px — every figure identical to the pre-change build. Against the loaded page the gap stays **0.58px** (67.58 / 263.58 / 227.58), the same sub-pixel drift `DESIGN.md` records on the fee row, neither introduced nor closed. **The literal reading was rejected on the kit's own test.** Emptying the inset so the ground stands alone as *the one rectangle* fails *"does what loads replace this surface, or sit inside it"* — the From/To text sits **inside**, so the strip is chrome and stays solid — and it would collapse each inset to its two 16px paddings, **32px**, taking **105px** out of the list. Recorded so it is not re-proposed as new. **One judgement beyond the selector, flagged:** her note was on card 1, but card 3's inset is the other shape (a single `Location`, not a route) and was left with two bars, which would have put two skeleton builds in one list. It took the same bar — same 35px, no reflow — so the three cards rehearse one rhythm. **One line to revert if she meant card 1 alone.** Conformance: the `<ul>` is `aria-hidden` and the page keeps its `role="status"` line *Loading your orders…* (`WCAG 4.1.3` ✓ — this screen never lost it, unlike `listings-loading` at rev 54); `1.4.11` is not engaged by a skeleton bar; the bar takes `--btn2` at `--r-detail` and joins the same pulse and the same `prefers-reduced-motion` stop as every other. **Kit consequence:** `.dr-sk-stop`, `.dr-sk-lbl`, `.dr-sk-place`, `.dr-sk-dir` and the `--to` label override are now **declared and spent nowhere** — this page was their only home — joining `.dr-btn--text`, `.dr-price--total`, `.dr-counter`, `--danger` and `--sh-onphoto` on the bench. Nothing deleted; the three size tokens still work, since the new bar computes its height from them. | `ui/kit.css`, `wireframes/order-history-loading.html`, `concept/concept.md` rev 64 |
| 2026-08-15 | **The empty mark becomes a drone with a line through it; the puzzle piece lasted hours.** Designer's call on `listings-empty`: *«постав на це місце ось цього дрона закресленого»*, with a Figma node showing a photographic quadcopter. **Reported before anything moved:** her snapshot captured `<span class="dr-mark">EMPTY</span>`, the text placeholder — but **rev 61 had already made it a puzzle piece** hours earlier in a parallel session, and her page was stale. Same as rev 56; the instruction stands either way and supersedes rev 61, whose entry stays on the record. **The reference is a photograph, the slot is an icon.** §6.3 floors a render at **~96px**; the mark is **52 × 52** with a **26px** glyph. A cutout at 26px is mush and puts two registers in one component, so this follows rev 61's own trade — *drawn in the one system, not imported*. **Chosen by rendering at final size and again at 6×, seven candidates, the rev-38 method:** front-view drone (a crossed-out bag), X-quad with blade bars (busy), X-quad with a horizontal strike (the line joined the airframe), X-quad unstruck (the best *drone*, nothing negating it), X-quad with a **steep 61°** strike — built, put on the page, then rejected on its own 6× render for running tangent to two rotors and merging into the top-right one — and the winner, the **plus-armed quad with the kit's own 45° slash**. **No departure, and that is why the airframe is a plus:** on an X the kit's slash lies *along an arm* and cancels nothing; on the axes the diagonal is empty, so the strike is `M4 4l16 16`, **byte-identical to `delivery-error`'s since 2026-08-03**, clearing all four rotors by **4.53** against `r 1.8`. The geometry was fitted to the rule rather than the rule bent to the geometry. **The slash is right here where §0.2 bars it on the wi-fi fan** — that rule is glyph-specific: on a *signal* a slash reads *disabled*, on an *object* it reads *absent*, which is `delivery-error`'s own reading since 2026-08-03. **Measured:** neutral `.dr-mark`, `--slate` on `--media` **5.51:1** (`WCAG 1.4.11` ✓) and correctly not `--danger` — an empty result is not an error; 26px glyph so the 1.7 stroke renders **1.84**, inside 0.85–2.27; `aria-hidden` with `role="status"` beside it. No new token, size or colour. **Open:** §6.2 assigns **`drone-10.png`** to *"Empty states — nothing ordered yet"*, so if she wants the photograph rather than the glyph it is a different **element** — a well at the ~96px floor — and striking through a photograph is unbuilt ground (`--sh-onphoto` is the only over-a-photo token and is spent nowhere). | `wireframes/listings-empty.html`, `concept/concept.md` §0.2 + rev 67 |
| 2026-08-03 | **Three Client components approved and built into the kit** — the listing card, the choice card, the upload zone — each proposed first, confirmed by the designer, then added to `kit.css`, the `kit.html` showcase and this file. One proposal correction recorded: the listing card was offered with a price row and a chevron, and the hand-edited frame has neither — the frame wins. | `ui/kit.css`, `ui/kit.html` |
| 2026-08-03 | **The ten painted pages now wear the kit.** `order-history` ×3, `tracking` ×4 and `delivery` ×3 link `ui/kit.css` and draw every component through `dr-*` classes; the per-page `<style>` blocks (13-19 KB each) are gone, so the byte-identical-block contract is superseded by one literal shared file. Verified element-by-element in the browser: **911 elements × 22 properties, before against after** — the only rendered change is the chip glyph 13 → 14 on the tracking pages, which is the designer's own earlier ruling landing. Three wf hooks stay by design: `wf-frame` (viewport switcher), `topbar` (EN/UA injection), `zlabel` (annotation toggle). Parity forced five kit corrections (skeleton well is `--media` not `--btn2`; the zone label is uppercase with a −4px bottom margin; the row panel's children stretch; the banner's action clearance is 4; the map well drops the cutout's inset) and surfaced **two recorded drifts for the designer**: the tracking family draws its skeleton bar at `--r-pill` where order-history draws 4px, and the loading line speaks twice — mono 11 (`.dr-hint`) on tracking/delivery, plain 14 (`.dr-status-line`) on order-history. | `wireframes/*`, `ui/kit.css` |
| 2026-08-02 | **The fee caption is removed.** `Delivery fee` and `Total` came off the four order cards on `order-history`; the figure now stands alone beside the ETA chip or the card actions. The words survive as an `.sr-only` accessible name, because a bare figure has no programmatic relationship without one (`WCAG 1.3.1`) — the visual goes, the meaning does not. The `.sk-fee-lbl` bar came off all three skeleton cards in the same pass, or the loading card would have stopped measuring the loaded one. **Nothing moved:** the fee row's height is set by the 44px card actions, not by the money column, so the delivered card is still **263.6px** against the skeleton's **263.0** — the same 0.6px `DESIGN.md` records. **Flagged and overruled:** the caption was the only thing distinguishing a live card's *fee* (₴180) from a delivered card's *total* (₴800); both are now bare figures. | `wireframes/order-history.html`, `-loading`, `DESIGN.md`, `ui/kit.css` |
| 2026-08-02 | **The nav bar is 56px, and the documents are corrected to the frames.** Measured by computed style on all ten painted pages. This inventory's Nav bar row said **44**; `DESIGN.md` did not state a height at all. The frames are the design, so both documents now carry 56. | `DESIGN.md`, `ui/inventory.md` |
| 2026-08-02 | **A chip's glyph is 14px, everywhere.** The build drew **13** in a badge (`.badge`, operator card) and **14** in the ETA pill (`.ohl-eta`, order card) — the same component at two values, against the One-Component-One-Value rule. The designer's call is 14. `--ic-13` becomes a declared rung that nothing spends. | `DESIGN.md`, `ui/inventory.md`, `ui/kit.css` |
| 2026-08-02 | **The chevron rotates 180° on open** — closed points down, open points up. It joins the button's `transform .12s` rather than becoming a fifth motion loop, because it answers a touch instead of carrying continuous information. Under `prefers-reduced-motion` the glyph still turns, it just does not travel: the direction is information, the animation is only how it gets there. | `concept.md` rev 44 |
| 2026-08-02 | **The brand slot follows the back control, not the title — 8 screens → 18.** The morning's rule was "no logo where there is a title"; the designer widened it to "no logo where there is a **back**". A tab root has a title but nothing to return to, so its bar has a free leading edge and the mark takes it, with the title 8px behind. The 21 screens that carry a `‹ Back` still do not get it: `HIG · Navigation bars` gives that bar to the back control and the title, and a mark wedged between them makes the leading edge argue with itself. Space was never the objection and was measured before the rule was written — at 375px the tightest tab root leaves **148px** free after the mark, 0 of 23 files overflow, and `WCAG` has no objection either way (**15.33 : 1** on the wireframe bar, **15.99 : 1** on the painted one, against 1.4.11's 3:1). This is the earlier rule narrowed, not dropped. | `wireframes/_conventions.md` §8 |
| 2026-08-02 | **`payment-loading` was left out of the widening, and it exposes a defect.** It is the only file in the product with neither a `‹ Back` nor a mark: its siblings `payment` and `payment-error` both carry `‹ Order review`, so the loading state has lost its back control. Branding it would have propagated that — the screen is pushed, not a tab root. Against `_conventions.md` §12 ("same DOM structure as its sibling state pages") the missing back is the defect, not the missing mark. **Recorded, not fixed** — the wireframe is not changed for it. | `ui/inventory.md` |

---

## Sources

- **[`DESIGN.md`](../DESIGN.md)** — the token-level spec extracted from the painted screens: palette
  with every measured ratio, ten type steps, six radii, spacing scale, components, imagery.
- **[`concept/concept.md`](../concept/concept.md)** — the reasoning and the change log, including
  every revision that was tried and reverted, so a rejected idea is not re-proposed as a new one.
- **[`wireframes/_screens.md`](../wireframes/_screens.md)** — which screens exist and which of the
  four states is real per screen; the matrix this file's screen count comes from.
- **[`wireframes/_conventions.md`](../wireframes/_conventions.md)** — the wireframe contract.
- **[`voice.md`](../voice.md)** and **[`microcopy.md`](../microcopy.md)** — every string shown.
- **[`CLAUDE.md`](../CLAUDE.md)** — design standards and the standing HIG + WCAG conformance check.
- **[`visuals/README.md`](../visuals/README.md)** — how every image in the product is generated: the
  house-style prompt, the two registers (product render vs user upload), the measured colour and
  temperature contract, naming and output sizes.
- **[`visuals/gaps.md`](../visuals/gaps.md)** — all 84 wireframes audited slot by slot: what was
  placed, what still has to be generated, and which slots are glyphs that must never become pictures.

**Counted and measured 2026-08-02 against 84 files.** The wireframes are edited by hand and they are
the design — so when a component changes there, or a fourth screen is painted, this inventory is
wrong until it is regenerated. It is a view of the source, not the source.
